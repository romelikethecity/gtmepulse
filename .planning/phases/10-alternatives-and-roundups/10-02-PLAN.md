---
phase: 10-alternatives-and-roundups
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - scripts/build.py
  - content/roundups_startup.py
  - content/roundups_enterprise.py
  - content/roundups_free.py
  - content/roundups_category.py
autonomous: true
requirements: [TBST-01, TBST-02, TBST-03, TBST-04, TBST-05, TBST-06, TBST-07, TBST-08, TBST-09, TBST-10]

must_haves:
  truths:
    - "User can visit /tools/best-gtm-tools-startups/ and read a ranked list of recommended tools with use-case guidance"
    - "10 best-for roundup pages exist covering startups, enterprise, free, enrichment, outbound, CRM, automation, AI, LinkedIn, and intent data"
    - "Every roundup page has a FAQ section with 3+ Q&A pairs and FAQPage JSON-LD schema"
    - "Roundup pages cross-link to individual tool reviews, comparison pages, and alternatives pages"
  artifacts:
    - path: "scripts/build.py"
      provides: "TOOL_ROUNDUPS data list, generate_tool_roundup(), _load_roundup_content(), build_tool_roundups(), roundup_related_links()"
      contains: "TOOL_ROUNDUPS"
    - path: "content/roundups_startup.py"
      provides: "Roundup content for startups and enterprise"
      contains: "ROUNDUPS"
    - path: "content/roundups_free.py"
      provides: "Roundup content for free tools and AI tools"
      contains: "ROUNDUPS"
    - path: "content/roundups_category.py"
      provides: "Roundup content for enrichment, outbound, CRM, automation, LinkedIn, intent data"
      contains: "ROUNDUPS"
  key_links:
    - from: "scripts/build.py"
      to: "content/roundups_*.py"
      via: "_load_roundup_content() importlib loader"
      pattern: "_load_roundup_content"
    - from: "generate_tool_roundup()"
      to: "faq_html, get_faq_schema, get_breadcrumb_schema"
      via: "function calls for schema markup"
      pattern: "faq_html|get_faq_schema|get_breadcrumb_schema"
    - from: "roundup_related_links()"
      to: "TOOL_ALTERNATIVES, TOOL_COMPARISONS"
      via: "cross-links to alternatives and comparisons"
      pattern: "TOOL_ALTERNATIVES|TOOL_COMPARISONS"
---

<objective>
Build 10 best-for roundup pages covering startup tools, enterprise tools, free tools, and category-specific recommendations, with ranked tool lists and use-case guidance.

Purpose: Capture "best [category] tools for GTM" search traffic with opinionated ranked lists that reference existing reviews, comparisons, and alternatives pages.
Output: 10 new pages at /tools/best-[topic]/, 3 content modules, generator infrastructure in build.py
</objective>

<execution_context>
@/Users/rome/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rome/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-alternatives-and-roundups/10-01-SUMMARY.md

<interfaces>
<!-- Plan 10-01 creates these, which this plan uses for cross-linking -->
TOOL_ALTERNATIVES list in build.py — 10 entries with slug, tool_name, category
BUILT_ALTERNATIVE_SLUGS set
_load_alternative_content(module_name, content_key)
generate_tool_alternative(alt)
alternative_related_links(current_slug, tool_name)

<!-- Existing infrastructure from Phases 8-9 -->
TOOL_REVIEWS list — 30 entries with slug, name, category
TOOL_COMPARISONS list — 20 entries with slug, tool_a, tool_b, category
TOOL_CATEGORIES list — 8 entries with slug, name
breadcrumb_html(), get_breadcrumb_schema(), faq_html(), get_faq_schema()
get_page_wrapper(), write_page(), pad_description()
newsletter_cta_html(), source_citation_html()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create roundup content modules for all 10 best-for pages</name>
  <files>content/roundups_startup.py, content/roundups_free.py, content/roundups_category.py</files>
  <action>
Create 3 content modules, each exporting a ROUNDUPS dict keyed by roundup slug.

Each ROUNDUPS entry has these keys:
- intro: 2-3 paragraphs framing the roundup (who it's for, what criteria matter, how tools were evaluated). 150-250 words.
- tools: ordered list (ranked) of 5-8 dicts, each with: rank (int), name, slug (matching TOOL_REVIEWS slug if review exists, None otherwise), category_tag (e.g. "Data Enrichment"), best_for (one sentence), why_picked (2-3 sentences explaining the recommendation), pricing (string), link_to_review (bool, True if review page exists)
- verdict: 2-3 paragraphs summarizing the top pick, runner-up, and budget option. Include a "quick pick" summary table in HTML.
- faq: list of 3-5 (question, answer) tuples

Content module assignments:

roundups_startup.py:
- best-gtm-tools-startups (TBST-01): Clay, Apollo, Instantly, HubSpot (free), Make, PostHog. Focus on cost-effective stack building.
- best-gtm-tools-enterprise (TBST-02): ZoomInfo, Outreach, Salesforce, 6sense, Segment. Focus on scale, compliance, support.

roundups_free.py:
- best-free-gtm-tools (TBST-03): Apollo (free tier), HubSpot CRM (free), n8n (self-hosted), PostHog (free tier), LinkedIn Sales Nav (free limited). Focus on $0 startup stack.
- best-ai-tools-gtm (TBST-08): Clay (AI enrichment), Persana, Apollo (AI sequences), ChatGPT/Claude for copy. Focus on AI-native workflows.

roundups_category.py (6 entries):
- best-data-enrichment-tools (TBST-04): Clay, Apollo, ZoomInfo, Clearbit, FullEnrich, Cognism, Lusha, LeadIQ
- best-outbound-sequencing-tools (TBST-05): Instantly, Smartlead, Outreach, Salesloft, Lemlist, Woodpecker
- best-crm-gtm-engineers (TBST-06): HubSpot, Salesforce, Attio, Close, Pipedrive
- best-workflow-automation-tools (TBST-07): Make, n8n, Zapier
- best-linkedin-prospecting-tools (TBST-09): LinkedIn Sales Navigator, HeyReach, PhantomBuster
- best-intent-data-platforms (TBST-10): 6sense, Bombora

For category roundups, every tool in the ranked list should reference its review page where one exists. Rankings should be opinionated with a clear #1 pick and reasoning.

Follow all CLAUDE.md writing rules: no banned words, no false reframes, no em-dashes, contractions always.
  </action>
  <verify>
    <automated>python3 -c "
import importlib.util, os
modules = ['roundups_startup', 'roundups_free', 'roundups_category']
expected = {
    'best-gtm-tools-startups': 'roundups_startup',
    'best-gtm-tools-enterprise': 'roundups_startup',
    'best-free-gtm-tools': 'roundups_free',
    'best-ai-tools-gtm': 'roundups_free',
    'best-data-enrichment-tools': 'roundups_category',
    'best-outbound-sequencing-tools': 'roundups_category',
    'best-crm-gtm-engineers': 'roundups_category',
    'best-workflow-automation-tools': 'roundups_category',
    'best-linkedin-prospecting-tools': 'roundups_category',
    'best-intent-data-platforms': 'roundups_category',
}
for mod_name in modules:
    path = os.path.join('content', f'{mod_name}.py')
    spec = importlib.util.spec_from_file_location(mod_name, path)
    mod = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(mod)
    assert hasattr(mod, 'ROUNDUPS'), f'{mod_name} missing ROUNDUPS dict'
for key, mod_name in expected.items():
    path = os.path.join('content', f'{mod_name}.py')
    spec = importlib.util.spec_from_file_location(mod_name, path)
    mod = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(mod)
    entry = mod.ROUNDUPS.get(key)
    assert entry, f'{key} not found in {mod_name}'
    assert 'intro' in entry, f'{key} missing intro'
    assert 'tools' in entry and len(entry['tools']) >= 3, f'{key} needs 3+ tools'
    assert 'verdict' in entry, f'{key} missing verdict'
    assert 'faq' in entry and len(entry['faq']) >= 3, f'{key} has fewer than 3 FAQ pairs'
print('All 10 roundup content modules validated')
"
    </automated>
  </verify>
  <done>3 content modules exist with ROUNDUPS dicts covering all 10 best-for topics, each with intro, ranked tool list, verdict with quick-pick table, and 3+ FAQ pairs</done>
</task>

<task type="auto">
  <name>Task 2: Add roundup generator infrastructure and data to build.py</name>
  <files>scripts/build.py</files>
  <action>
Add to build.py (insert after the alternatives infrastructure added by 10-01, before _load_review_content):

1. TOOL_ROUNDUPS data list — 10 entries, each dict with:
   - slug: e.g. "best-gtm-tools-startups"
   - title: e.g. "Best GTM Tools for Startups 2026"
   - meta_desc: 150-158 chars, action-oriented
   - content_module: e.g. "roundups_startup"
   - content_key: e.g. "best-gtm-tools-startups"
   - category_tag: e.g. "Startup Stack" (displayed as eyebrow)

   All 10 slugs:
   - best-gtm-tools-startups
   - best-gtm-tools-enterprise
   - best-free-gtm-tools
   - best-data-enrichment-tools
   - best-outbound-sequencing-tools
   - best-crm-gtm-engineers
   - best-workflow-automation-tools
   - best-ai-tools-gtm
   - best-linkedin-prospecting-tools
   - best-intent-data-platforms

2. BUILT_ROUNDUP_SLUGS = {r["slug"] for r in TOOL_ROUNDUPS}

3. _load_roundup_content(module_name, content_key) — clone _load_alternative_content but reads mod.ROUNDUPS

4. roundup_related_links(current_slug) — generates related links grid with:
   - Link to /tools/ index
   - Links to relevant alternatives pages (from TOOL_ALTERNATIVES)
   - Links to other roundup pages
   - Links to relevant category indexes
   - Cap at 12 links

5. generate_tool_roundup(roundup) — generates one roundup page. Structure:
   - Breadcrumbs: Home > Tools > [Title]
   - salary-header section with eyebrow (category_tag), h1 (title), subtitle
   - salary-content div with:
     - intro from content module
     - For each tool in ranked order: h2 with "#{rank}: [Tool Name]", category_tag badge, "Best for: [best_for]" paragraph, why_picked paragraph, pricing info. If link_to_review is True, link tool name to /tools/[slug]/
     - verdict section with quick-pick HTML table
     - FAQ section via faq_html() + faq_schema via get_faq_schema()
   - roundup_related_links()
   - source_citation_html()
   - newsletter_cta_html()
   - BreadcrumbList + FAQPage JSON-LD in extra_head
   - Use get_page_wrapper with active_path="/tools/"

6. build_tool_roundups() — iterates TOOL_ROUNDUPS, calls generate_tool_roundup for each in BUILT_ROUNDUP_SLUGS

7. In main(), add build_tool_roundups() call after build_tool_alternatives()

URL pattern: /tools/best-[topic]/ (e.g., /tools/best-gtm-tools-startups/)
  </action>
  <verify>
    <automated>cd /Users/rome/Documents/projects/gtmepulse && python3 scripts/build.py 2>&1 | tail -20</automated>
  </verify>
  <done>Build completes with 10 new roundup pages (211 total pages), all at /tools/best-[topic]/, each with FAQ schema, breadcrumbs, cross-links to reviews, comparisons, and alternatives, zero validation warnings</done>
</task>

</tasks>

<verification>
- `python3 scripts/build.py` completes with zero validation warnings
- 10 roundup pages exist in output/tools/best-[topic]/index.html
- Each page has FAQPage JSON-LD with 3+ Q&A pairs
- Each page has BreadcrumbList JSON-LD
- Pages link to tool review pages, comparison pages, and alternatives pages
- No banned words (QUAL2-09 check)
</verification>

<success_criteria>
- 10 roundup pages built and accessible at /tools/best-[topic]/
- Every page has ranked tool list with opinionated #1 pick
- FAQ sections present with matching JSON-LD schema
- Cross-links to reviews, comparisons, and alternatives work
- Build runs clean with no warnings
- Total site reaches ~211 pages
</success_criteria>

<output>
After completion, create `.planning/phases/10-alternatives-and-roundups/10-02-SUMMARY.md`
</output>
