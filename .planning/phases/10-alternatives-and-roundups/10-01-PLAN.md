---
phase: 10-alternatives-and-roundups
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build.py
  - content/alternatives_enrichment.py
  - content/alternatives_outbound.py
  - content/alternatives_crm.py
  - content/alternatives_automation.py
  - content/alternatives_intent.py
  - content/alternatives_linkedin.py
autonomous: true
requirements: [TALT-01, TALT-02, TALT-03, TALT-04, TALT-05, TALT-06, TALT-07, TALT-08, TALT-09, TALT-10]

must_haves:
  truths:
    - "User can visit /tools/clay-alternatives/ and read 5-8 alternative tools with pros, cons, and pricing for each of 10 tools"
    - "Every alternatives page has a FAQ section with 3+ Q&A pairs and FAQPage JSON-LD schema"
    - "Alternatives pages cross-link to individual tool review pages and comparison pages"
    - "Build completes cleanly with all 10 alternatives pages in the output"
  artifacts:
    - path: "scripts/build.py"
      provides: "TOOL_ALTERNATIVES data list, generate_tool_alternative(), _load_alternative_content(), build_tool_alternatives(), alternative_related_links()"
      contains: "TOOL_ALTERNATIVES"
    - path: "content/alternatives_enrichment.py"
      provides: "Alternatives content for Clay, Apollo, ZoomInfo"
      contains: "ALTERNATIVES"
    - path: "content/alternatives_outbound.py"
      provides: "Alternatives content for Instantly, Outreach"
      contains: "ALTERNATIVES"
    - path: "content/alternatives_crm.py"
      provides: "Alternatives content for HubSpot, Salesforce"
      contains: "ALTERNATIVES"
    - path: "content/alternatives_automation.py"
      provides: "Alternatives content for Zapier"
      contains: "ALTERNATIVES"
    - path: "content/alternatives_intent.py"
      provides: "Alternatives content for 6sense"
      contains: "ALTERNATIVES"
    - path: "content/alternatives_linkedin.py"
      provides: "Alternatives content for LinkedIn Sales Navigator"
      contains: "ALTERNATIVES"
  key_links:
    - from: "scripts/build.py"
      to: "content/alternatives_*.py"
      via: "_load_alternative_content() importlib loader"
      pattern: "_load_alternative_content"
    - from: "generate_tool_alternative()"
      to: "faq_html, get_faq_schema, get_breadcrumb_schema"
      via: "function calls for schema markup"
      pattern: "faq_html|get_faq_schema|get_breadcrumb_schema"
---

<objective>
Build 10 alternatives pages (Clay, Apollo, ZoomInfo, Instantly, Outreach, HubSpot, Salesforce, Zapier, 6sense, LinkedIn Sales Navigator alternatives) with content modules following the established comparison content module pattern.

Purpose: Capture "[Tool] alternatives" search traffic with opinionated, vendor-neutral recommendations that cross-link to existing reviews and comparisons.
Output: 10 new pages at /tools/[tool]-alternatives/, 6 content modules, generator infrastructure in build.py
</objective>

<execution_context>
@/Users/rome/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rome/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-tool-categories-and-comparisons/09-01-SUMMARY.md

<interfaces>
<!-- Established patterns from Phase 9 that this plan reuses -->

From scripts/build.py (comparison pattern to clone for alternatives):
- TOOL_COMPARISONS list of dicts: {slug, tool_a, tool_b, title, meta_desc, content_module, content_key, category}
- _load_comparison_content(module_name, content_key) — importlib.util loader returning mod.COMPARISONS.get(content_key)
- generate_tool_comparison(comp) — breadcrumbs, FAQ schema, affiliate disclosure, salary-header/salary-content CSS
- build_tool_comparisons() — iterates TOOL_COMPARISONS, checks BUILT_COMPARISON_SLUGS
- tool_comparison_related_links(current_slug, category) — related links grid

From scripts/build.py (key helpers):
- breadcrumb_html(crumbs), get_breadcrumb_schema(crumbs)
- faq_html(faq_pairs), get_faq_schema(faq_pairs)
- get_page_wrapper(title, description, canonical_path, body_content, active_path, extra_head, body_class)
- write_page(filepath, page)
- pad_description(desc)
- newsletter_cta_html(context)
- source_citation_html()

From content/comparisons_enrichment.py (content module pattern):
- COMPARISONS dict keyed by slug, each value has: intro, feature_table, tool_a_strengths, tool_b_strengths, pricing_comparison, verdict, faq

Affiliate tools set: {"clay", "apollo", "instantly", "smartlead", "lemlist"}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create alternatives content modules for all 10 tools</name>
  <files>content/alternatives_enrichment.py, content/alternatives_outbound.py, content/alternatives_crm.py, content/alternatives_automation.py, content/alternatives_intent.py, content/alternatives_linkedin.py</files>
  <action>
Create 6 content modules following the COMPARISONS dict pattern from comparisons_*.py files. Each module exports an ALTERNATIVES dict keyed by tool slug.

Each ALTERNATIVES entry has these keys:
- intro: 2-3 paragraphs explaining why users look for alternatives to this tool (150-250 words). Be specific about the pain points (pricing, complexity, missing features, vendor lock-in).
- alternatives: list of 5-8 dicts, each with: name, slug (matching TOOL_REVIEWS slug if review exists, None otherwise), tagline (one-liner), best_for (one sentence), pros (list of 3-4 strings), cons (list of 2-3 strings), pricing (string like "$49/mo" or "Free tier available"), verdict (2-3 sentences on when to pick this tool)
- faq: list of 3-5 (question, answer) tuples covering "Is [tool] worth it?", "What's the best free alternative?", "Can I migrate from [tool]?", etc.

Content module assignments:
- alternatives_enrichment.py: clay, apollo, zoominfo (TALT-01, TALT-02, TALT-03)
- alternatives_outbound.py: instantly, outreach (TALT-04, TALT-05)
- alternatives_crm.py: hubspot, salesforce (TALT-06, TALT-07)
- alternatives_automation.py: zapier (TALT-08)
- alternatives_intent.py: 6sense (TALT-09)
- alternatives_linkedin.py: linkedin-sales-navigator (TALT-10)

For each alternatives page, the 5-8 alternatives MUST reference tools that have review pages in TOOL_REVIEWS where possible (use their slug). Include 1-2 tools outside the reviewed set for completeness.

Example alternatives for Clay: Apollo, ZoomInfo, Clearbit, FullEnrich, Persana, Lusha, Cognism (all have reviews). For 6sense: Bombora (has review), G2, TrustRadius, Demandbase, LeadSift (no reviews, slug=None).

Follow all CLAUDE.md writing rules: no banned words, no false reframes, no em-dashes, contractions always.
  </action>
  <verify>
    <automated>python3 -c "
import importlib.util, os
modules = ['alternatives_enrichment', 'alternatives_outbound', 'alternatives_crm', 'alternatives_automation', 'alternatives_intent', 'alternatives_linkedin']
expected_keys = {'clay': 'alternatives_enrichment', 'apollo': 'alternatives_enrichment', 'zoominfo': 'alternatives_enrichment', 'instantly': 'alternatives_outbound', 'outreach': 'alternatives_outbound', 'hubspot': 'alternatives_crm', 'salesforce': 'alternatives_crm', 'zapier': 'alternatives_automation', '6sense': 'alternatives_intent', 'linkedin-sales-navigator': 'alternatives_linkedin'}
for mod_name in modules:
    path = os.path.join('content', f'{mod_name}.py')
    spec = importlib.util.spec_from_file_location(mod_name, path)
    mod = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(mod)
    assert hasattr(mod, 'ALTERNATIVES'), f'{mod_name} missing ALTERNATIVES dict'
for key, mod_name in expected_keys.items():
    path = os.path.join('content', f'{mod_name}.py')
    spec = importlib.util.spec_from_file_location(mod_name, path)
    mod = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(mod)
    entry = mod.ALTERNATIVES.get(key)
    assert entry, f'{key} not found in {mod_name}'
    assert 'intro' in entry, f'{key} missing intro'
    assert 'alternatives' in entry, f'{key} missing alternatives'
    assert len(entry['alternatives']) >= 5, f'{key} has fewer than 5 alternatives'
    assert 'faq' in entry and len(entry['faq']) >= 3, f'{key} has fewer than 3 FAQ pairs'
print('All 10 alternatives content modules validated')
"
    </automated>
  </verify>
  <done>6 content modules exist with ALTERNATIVES dicts covering all 10 tools, each with intro, 5-8 alternatives (name, pros, cons, pricing, verdict), and 3+ FAQ pairs</done>
</task>

<task type="auto">
  <name>Task 2: Add alternatives generator infrastructure and data to build.py</name>
  <files>scripts/build.py</files>
  <action>
Add to build.py (insert after the TOOL_COMPARISONS/build_tool_comparisons block, before the _load_review_content function around line 6316):

1. TOOL_ALTERNATIVES data list — 10 entries, each dict with:
   - slug: e.g. "clay-alternatives"
   - tool_name: e.g. "Clay"
   - title: e.g. "Best Clay Alternatives 2026 for GTM Engineers"
   - meta_desc: 150-158 chars, action-oriented
   - content_module: e.g. "alternatives_enrichment"
   - content_key: e.g. "clay"
   - category: matching TOOL_CATEGORIES category name

   All 10 tools: clay, apollo, zoominfo, instantly, outreach, hubspot, salesforce, zapier, 6sense, linkedin-sales-navigator

2. BUILT_ALTERNATIVE_SLUGS = {a["slug"] for a in TOOL_ALTERNATIVES}

3. _load_alternative_content(module_name, content_key) — clone _load_comparison_content but reads mod.ALTERNATIVES instead of mod.COMPARISONS

4. alternative_related_links(current_slug, tool_name) — generates related links grid with:
   - Link to /tools/ index
   - Link to the main tool's review page (if exists in TOOL_REVIEWS)
   - Links to comparisons involving this tool (from TOOL_COMPARISONS)
   - Links to other alternatives pages
   - Cap at 12 links

5. generate_tool_alternative(alt) — generates one alternatives page. Structure:
   - Breadcrumbs: Home > Tools > [Tool] Alternatives
   - salary-header section with eyebrow (category), h1 ("[Tool] Alternatives"), subtitle
   - salary-content div with:
     - intro section from content module
     - For each alternative: h2 with tool name, "Best for: [best_for]" paragraph, pros/cons as styled lists (use existing list styles), pricing paragraph, verdict paragraph. If the alternative has a slug (review exists), link the h2 to /tools/[slug]/
     - FAQ section via faq_html() + faq_schema via get_faq_schema()
   - Affiliate disclosure if tool slug matches affiliate set
   - alternative_related_links()
   - source_citation_html()
   - newsletter_cta_html()
   - BreadcrumbList + FAQPage JSON-LD in extra_head
   - Use get_page_wrapper with active_path="/tools/"

6. build_tool_alternatives() — iterates TOOL_ALTERNATIVES, calls generate_tool_alternative for each in BUILT_ALTERNATIVE_SLUGS

7. In main(), add build_tool_alternatives() call after build_tool_comparisons() (around line 11787)

URL pattern: /tools/[tool]-alternatives/ (e.g., /tools/clay-alternatives/)
  </action>
  <verify>
    <automated>cd /Users/rome/Documents/projects/gtmepulse && python3 scripts/build.py 2>&1 | tail -20</automated>
  </verify>
  <done>Build completes with 10 new alternatives pages (201 total pages), all at /tools/[tool]-alternatives/, each with FAQ schema, breadcrumbs, cross-links to reviews and comparisons, zero validation warnings</done>
</task>

</tasks>

<verification>
- `python3 scripts/build.py` completes with zero validation warnings
- 10 alternatives pages exist in output/tools/[tool]-alternatives/index.html
- Each page has FAQPage JSON-LD with 3+ Q&A pairs
- Each page has BreadcrumbList JSON-LD
- Pages link to tool review pages and comparison pages
- No banned words (QUAL2-09 check in validate_pages)
</verification>

<success_criteria>
- 10 alternatives pages built and accessible at /tools/[tool]-alternatives/
- Every page has 5-8 alternatives with pros, cons, pricing, and verdict
- FAQ sections present with matching JSON-LD schema
- Cross-links to existing reviews and comparisons work
- Build runs clean with no warnings
</success_criteria>

<output>
After completion, create `.planning/phases/10-alternatives-and-roundups/10-01-SUMMARY.md`
</output>
