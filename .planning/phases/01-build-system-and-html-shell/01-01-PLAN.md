---
phase: 01-build-system-and-html-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/nav_config.py
  - scripts/templates.py
  - assets/css/components.css
  - assets/css/styles.css
autonomous: true
requirements:
  - BUILD-02
  - BUILD-03
  - HTML-01
  - HTML-02
  - HTML-03
  - HTML-04
  - HTML-05
  - SEO-01
  - SEO-02
  - SEO-03
  - SEO-04

must_haves:
  truths:
    - "nav_config.py exports all site constants, NAV_ITEMS, and FOOTER_COLUMNS with zero logic"
    - "templates.py produces valid HTML documents with correct head, nav, footer, and wrapper"
    - "Schema helpers generate valid JSON-LD via json.dumps() for Organization+WebSite, BreadcrumbList, and FAQPage"
    - "CSS cascade works: tokens.css variables load, components.css provides reusable classes, styles.css adds page-scoped styles"
    - "Nav collapses to hamburger at 768px with working toggle"
  artifacts:
    - path: "scripts/nav_config.py"
      provides: "Site constants, NAV_ITEMS, FOOTER_COLUMNS, CSS_VERSION"
      min_lines: 80
    - path: "scripts/templates.py"
      provides: "get_html_head, get_nav_html, get_footer_html, get_page_wrapper, write_page, schema helpers"
      min_lines: 250
    - path: "assets/css/components.css"
      provides: "Reusable component classes (cards, buttons, tables, breadcrumbs, stats, FAQ)"
      min_lines: 100
    - path: "assets/css/styles.css"
      provides: "Nav, footer, page-header, responsive breakpoints"
      min_lines: 150
  key_links:
    - from: "scripts/templates.py"
      to: "scripts/nav_config.py"
      via: "import"
      pattern: "from nav_config import"
    - from: "assets/css/components.css"
      to: "assets/css/tokens.css"
      via: "CSS custom properties"
      pattern: "var\\(--gtme-"
    - from: "scripts/templates.py"
      to: "assets/css/components.css"
      via: "CSS class references in HTML"
      pattern: "class=\"(btn|card|stat|breadcrumb|faq|newsletter)"
---

<objective>
Create the configuration layer (nav_config.py), HTML shell layer (templates.py), and CSS styling layers (components.css + styles.css) that every page in the site depends on.

Purpose: These 4 files are the foundation. build.py (Plan 02) imports from nav_config.py and templates.py to generate pages. Every page rendered uses the CSS cascade. Nothing can be built without these.

Output: 4 files that, when imported/linked, produce a complete, responsive, schema-marked HTML page from any body content string.
</objective>

<execution_context>
@/Users/rome/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rome/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-build-system-and-html-shell/01-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@CLAUDE.md
@assets/css/tokens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create nav_config.py and templates.py</name>
  <files>scripts/nav_config.py, scripts/templates.py</files>
  <action>
**nav_config.py** (~100 lines, pure data, zero imports):

Create with these exact exports:
- `SITE_NAME = "GTME Pulse"`
- `SITE_URL = "https://gtmepulse.com"`
- `SITE_TAGLINE = "Career intelligence for GTM Engineers"`
- `COPYRIGHT_YEAR = "2026"`
- `CURRENT_YEAR = 2026`
- `CSS_VERSION = "1"`
- `CTA_HREF = "/newsletter/"`
- `CTA_LABEL = "Get the Weekly Pulse"`
- `NAV_ITEMS` list with: Salary Data (dropdown with children: Salary Index, By Seniority, By Location, By Company Stage, Comparisons, Salary Calculator), Tools, Careers, Newsletter
- `FOOTER_COLUMNS` dict with 3 columns: "Salary Data" (5 links), "Resources" (4 links: GTM Tools, Career Guides, Newsletter, About), "Site" (3 links: Privacy Policy, Terms of Service, Methodology)

Use exact structure from research doc Pattern 1 example.

**templates.py** (~350-400 lines):

Module-level state:
- `ALL_PAGES = []` (mutable list for sitemap tracking)
- `OUTPUT_DIR = ""` (set by build.py at startup)
- Import only from nav_config.py: `from nav_config import *`
- Import stdlib: `os`, `json`

Functions to implement:

1. `get_html_head(title, description, canonical_path, extra_head="")` - Use exact pattern from research doc Code Examples section. Full title = `{title} - {SITE_NAME}` unless title == SITE_NAME. Includes: charset, viewport, theme-color (#FF4F1F), title, meta description, canonical, robots meta, OG tags (type/url/title/description/site_name), Twitter Card (summary_large_image), all favicon links (paths: /assets/favicons/favicon.svg, .ico, -32x32.png, -16x16.png, apple-touch-icon.png, site.webmanifest), Google Fonts single combined request (Sora:400-700, Plus Jakarta Sans:400-800, Source Code Pro:400-700 with display=swap), two preconnect hints, and three CSS links (tokens.css without cache bust, components.css?v={CSS_VERSION}, styles.css?v={CSS_VERSION}).

2. `get_nav_html(active_path="")` - Responsive nav with: site logo as img tag (`/assets/logos/logo-horizontal-light.svg`, alt="GTME Pulse", class="nav-logo"), nav links from NAV_ITEMS with dropdown support (items with `children` get a dropdown toggle button and child list), mobile hamburger button (class="nav-mobile-toggle", aria-label="Menu"), CTA button (class="btn btn--primary nav-cta", href=CTA_HREF, text=CTA_LABEL). Active page gets class="active" on its nav link. Use classes: .site-nav, .nav-container, .nav-brand, .nav-links, .nav-item, .nav-item--dropdown, .nav-dropdown, .nav-mobile-toggle, .nav-cta.

3. `get_footer_html()` - Multi-column footer with: footer columns from FOOTER_COLUMNS (each column is h4 + ul of links), newsletter mini-form (email input + submit button, with placeholder "Your email" and button text "Subscribe", wrapped in form with class="footer-newsletter-form" -- form is non-functional in Phase 1, JS handler added in Phase 2), bottom bar with copyright (Copyright 2026 GTME Pulse. All rights reserved.) and SITE_TAGLINE. Use classes: .site-footer, .footer-container, .footer-grid, .footer-column, .footer-newsletter, .footer-bottom.

4. `get_page_wrapper(title, description, canonical_path, body_content, active_path="", extra_head="", body_class="")` - Assembles full HTML document: get_html_head() + `<body class="{body_class}">` + get_nav_html() + `<main class="main-content">{body_content}</main>` + get_footer_html() + inline JS for mobile nav toggle (classList.toggle on .nav-links and .nav-mobile-toggle when hamburger clicked) + dropdown toggle behavior (click toggles .open class on parent .nav-item--dropdown) + `</body></html>`.

5. `write_page(rel_path, content)` - Write HTML file to OUTPUT_DIR/rel_path, create dirs with os.makedirs(exist_ok=True), append rel_path to ALL_PAGES.

6. `get_homepage_schema()` - Organization + WebSite @graph JSON-LD. Use json.dumps(). Organization: name=SITE_NAME, url=SITE_URL, description=SITE_TAGLINE, logo=SITE_URL/assets/logos/icon-mark.svg. WebSite: name=SITE_NAME, url=SITE_URL, description=SITE_TAGLINE. Return as `<script type="application/ld+json">` tag.

7. `get_breadcrumb_schema(items)` - BreadcrumbList JSON-LD. items = [(label, url), ...]. Use exact pattern from research doc. json.dumps().

8. `get_faq_schema(qa_pairs)` - FAQPage JSON-LD. qa_pairs = [(question, answer), ...]. Min 3 pairs. json.dumps(). Return as script tag.

9. `breadcrumb_html(crumbs)` - Visual breadcrumb nav. crumbs = [(label, url), ...], last item is current page (no link). Use exact pattern from research doc.

10. `newsletter_cta_html(context="")` - Email capture block with heading, description, email input, submit button. Non-functional in Phase 1 (no JS handler). class="newsletter-cta".

CRITICAL import pattern: templates.py imports from nav_config only. build.py imports from both. Data flows one direction: build.py -> templates.py via function arguments. OUTPUT_DIR is set via `templates.OUTPUT_DIR = OUTPUT_DIR` from build.py.
  </action>
  <verify>
    <automated>cd /Users/rome/Documents/projects/gtmepulse && python3 -c "
import sys; sys.path.insert(0, 'scripts')
from nav_config import SITE_NAME, SITE_URL, NAV_ITEMS, FOOTER_COLUMNS, CSS_VERSION, CTA_HREF, CTA_LABEL
from templates import get_html_head, get_nav_html, get_footer_html, get_page_wrapper, write_page, ALL_PAGES
from templates import get_homepage_schema, get_breadcrumb_schema, get_faq_schema, breadcrumb_html, newsletter_cta_html
import json, tempfile, os
# Test head generation
head = get_html_head('Test Page', 'A test description for this page.', '/test/')
assert 'Test Page - GTME Pulse' in head, 'Title format wrong'
assert 'content=\"A test' in head, 'Missing description'
assert 'gtmepulse.com/test/' in head, 'Missing canonical'
assert 'tokens.css' in head, 'Missing tokens.css'
assert 'components.css?v=' in head, 'Missing cache-busted components.css'
assert 'fonts.googleapis.com' in head, 'Missing Google Fonts'
assert 'favicon.svg' in head, 'Missing favicon'
# Test nav
nav = get_nav_html()
assert 'logo-horizontal-light.svg' in nav, 'Missing logo'
assert 'nav-mobile-toggle' in nav, 'Missing hamburger'
assert CTA_LABEL in nav, 'Missing CTA'
# Test footer
footer = get_footer_html()
assert 'footer-newsletter' in footer.lower() or 'footer-newsletter-form' in footer.lower(), 'Missing newsletter form'
assert '2026' in footer, 'Missing copyright year'
# Test schema
schema = get_homepage_schema()
assert 'Organization' in schema, 'Missing Organization schema'
assert 'WebSite' in schema, 'Missing WebSite schema'
assert 'application/ld+json' in schema, 'Missing script tag'
bc = get_breadcrumb_schema([('Home', '/'), ('About', None)])
assert 'BreadcrumbList' in bc, 'Missing BreadcrumbList'
faq = get_faq_schema([('Q1?', 'A1'), ('Q2?', 'A2'), ('Q3?', 'A3')])
assert 'FAQPage' in faq, 'Missing FAQPage'
# Test write_page
import templates
templates.OUTPUT_DIR = tempfile.mkdtemp()
write_page('test/index.html', '<html>test</html>')
assert 'test/index.html' in ALL_PAGES, 'write_page did not register'
assert os.path.exists(os.path.join(templates.OUTPUT_DIR, 'test/index.html')), 'File not written'
print('ALL CHECKS PASSED')
"
    </automated>
  </verify>
  <done>nav_config.py exports all constants and nav/footer data. templates.py exports all 10 functions. Imports are one-directional (templates imports from nav_config only). All schema helpers use json.dumps(). write_page() tracks in ALL_PAGES.</done>
</task>

<task type="auto">
  <name>Task 2: Create components.css and styles.css</name>
  <files>assets/css/components.css, assets/css/styles.css</files>
  <action>
**components.css** (~200 lines) - Reusable component classes. Every color/spacing/font/radius/shadow value MUST reference a --gtme-* token from tokens.css. Zero hardcoded hex codes. Zero !important.

Include these components (use exact skeleton from research doc CSS Component Skeleton, expanded):

1. **Reset**: `*, *::before, *::after { box-sizing: border-box; }`, body reset (margin:0, font-family var(--gtme-font-body), color var(--gtme-text-primary), bg var(--gtme-bg-primary), line-height 1.65, font-size var(--gtme-text-base))
2. **Container**: .container (max-width 1200px, margin 0 auto, padding 0 var(--gtme-space-6))
3. **Buttons**: .btn (inline-flex, centered, gap, padding space-3/space-6, radius-md, font-body, semibold, text-sm, no border, pointer, transition), .btn--primary (accent bg, text-on-accent, hover: accent-hover + shadow-accent), .btn--ghost (transparent, text-primary, 1px border)
4. **Cards**: .card (bg-surface, border-subtle 1px, radius-lg, padding space-6), .card-grid (CSS grid, auto-fill minmax(300px, 1fr), gap space-6)
5. **Stat blocks**: .stat-grid (grid auto-fit minmax(150px, 1fr), gap space-4), .stat-block (text-align center), .stat-value (font-mono, text-2xl, bold, accent color), .stat-label (text-sm, text-secondary, margin-top space-1)
6. **Breadcrumb**: .breadcrumb (text-sm, text-secondary, margin-bottom space-4), .breadcrumb-link (text-secondary, no underline, hover: accent), .breadcrumb-sep (margin 0 space-2), .breadcrumb-current (text-primary)
7. **Tables**: .data-table (width 100%, border-collapse), th/td (padding space-3/space-4, text-align left, border-bottom border-subtle), th (semibold, text-sm, text-secondary)
8. **FAQ**: .faq-section (margin-top space-12), .faq-item (border-bottom border-subtle, padding space-4 0), .faq-question (font-heading, semibold), .faq-answer (text-secondary, margin-top space-2)
9. **Newsletter CTA**: .newsletter-cta (bg-tinted, radius-lg, padding space-8, text-align center)
10. **Page header**: .page-header (padding space-12 0 space-8), .page-header h1 (font-heading, bold, letter-spacing -0.5px)
11. **Related links**: .related-links (margin-top space-12, padding-top space-8, border-top border-subtle)
12. **Links**: a (color accent, text-decoration none), a:hover (color accent-hover)
13. **Headings**: h1-h6 (font-heading, font-weight bold, color text-primary, line-height 1.2)

**styles.css** (~300 lines) - Page-scoped and structural styles. Same rules: only --gtme-* tokens.

Sections:

1. **Site nav** (.site-nav): sticky top, bg-surface, border-bottom border-subtle, z-sticky. .nav-container: max-width 1200px, margin 0 auto, flex, align-center, justify-between, padding space-3 space-6. .nav-brand: flex align-center. .nav-logo: height 32px, width auto. .nav-links: flex, gap space-1, list-style none, margin 0, padding 0. .nav-item a: padding space-2 space-3, text-sm, text-secondary, no underline, radius-sm, hover bg-tinted. .nav-item a.active: text-primary, font-weight semibold. .nav-item--dropdown: position relative. .nav-dropdown: position absolute, top 100%, left 0, bg-surface, border border-subtle, radius-md, shadow-md, padding space-2, min-width 200px, display none, z-dropdown. .nav-item--dropdown.open .nav-dropdown: display block. .nav-mobile-toggle: display none (shown via media query). .nav-cta: margin-left space-4.

2. **Footer** (.site-footer): bg-dark, color text-inverse, padding space-16 0 space-8. .footer-container: max-width 1200px, margin 0 auto, padding 0 space-6. .footer-grid: display grid, grid-template-columns repeat(auto-fit, minmax(200px, 1fr)), gap space-8. .footer-column h4: font-heading, text-sm, text-secondary (dark mode secondary), margin-bottom space-4, text-transform uppercase, letter-spacing 1px. .footer-column ul: list-style none, padding 0. .footer-column a: color text-inverse, opacity 0.8, hover opacity 1. .footer-newsletter: grid-column span all on wide screens. .footer-newsletter-form: flex gap space-2, max-width 400px. .footer-newsletter-form input: flex 1, padding space-2 space-3, radius-md, border none, font-body. .footer-newsletter-form button: btn btn--primary styling. .footer-bottom: margin-top space-12, padding-top space-6, border-top 1px solid rgba(255,255,255,0.1), flex, justify-between, text-sm, opacity 0.6.

3. **Main content**: .main-content (padding space-8 0, min-height calc(100vh - nav height - footer estimate)).

4. **Page types**: .page-header (max-width 800px for readability). Body class scoping: body.page-home, body.page-inner for future page-type styles.

5. **Responsive breakpoints** (at bottom of file):
   - `@media (max-width: 768px)`: .nav-links (display none when not .open, flex-direction column, position absolute, top 100%, left 0, right 0, bg-surface, border-bottom border-subtle, padding space-4). .nav-mobile-toggle (display flex, background none, border none, padding space-2, cursor pointer, font-size 1.5rem). .nav-links.open (display flex). .nav-dropdown (position static, box-shadow none, border none, padding-left space-4). .nav-cta (width 100%, justify-content center, margin-left 0, margin-top space-2). .footer-grid (grid-template-columns 1fr 1fr). .stat-grid (grid-template-columns 1fr 1fr). .data-table (display block, overflow-x auto for horizontal scroll -- card stack conversion deferred to Phase 3 QUAL-06).
   - `@media (max-width: 480px)`: .footer-grid (grid-template-columns 1fr). .stat-grid (grid-template-columns 1fr). .container (padding 0 space-4).
  </action>
  <verify>
    <automated>cd /Users/rome/Documents/projects/gtmepulse && python3 -c "
import re
# Check components.css
with open('assets/css/components.css') as f:
    comp = f.read()
assert 'var(--gtme-' in comp, 'components.css missing token references'
assert '.btn' in comp, 'Missing .btn'
assert '.card' in comp, 'Missing .card'
assert '.stat-grid' in comp, 'Missing .stat-grid'
assert '.breadcrumb' in comp, 'Missing .breadcrumb'
assert '.faq-' in comp, 'Missing .faq classes'
assert '.newsletter-cta' in comp, 'Missing .newsletter-cta'
assert '!important' not in comp, 'components.css has !important'
# No hardcoded hex except in comments
lines = [l for l in comp.split('\n') if not l.strip().startswith('/*') and not l.strip().startswith('*')]
code_only = '\n'.join(lines)
hex_matches = re.findall(r':\s*#[0-9a-fA-F]{3,8}', code_only)
assert len(hex_matches) == 0, f'Hardcoded hex in components.css: {hex_matches}'

# Check styles.css
with open('assets/css/styles.css') as f:
    sty = f.read()
assert '.site-nav' in sty, 'Missing .site-nav'
assert '.site-footer' in sty, 'Missing .site-footer'
assert '.nav-mobile-toggle' in sty, 'Missing mobile toggle styles'
assert '@media' in sty, 'Missing responsive breakpoints'
assert '768px' in sty, 'Missing 768px breakpoint'
assert '!important' not in sty, 'styles.css has !important'

print('ALL CSS CHECKS PASSED')
"
    </automated>
  </verify>
  <done>components.css provides reusable classes for cards, buttons, stats, breadcrumbs, tables, FAQ, newsletter CTA, page headers, and related links. styles.css provides nav, footer, main content, and responsive breakpoints at 768px and 480px. All values reference --gtme-* tokens. Zero hardcoded colors. Zero !important.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `python3 -c "import sys; sys.path.insert(0,'scripts'); from templates import get_page_wrapper; print(get_page_wrapper('Test','Desc','/test/','<h1>Hello</h1>')[:200])"` produces valid HTML opening
2. All CSS files load without syntax errors (browser dev tools, no parse errors)
3. tokens.css variables are referenced (not redefined) in components.css and styles.css
</verification>

<success_criteria>
- nav_config.py is pure data, zero logic, exports all site constants and nav/footer structures
- templates.py exports all 10 functions, imports only from nav_config.py, tracks pages in ALL_PAGES
- All 3 schema helpers (homepage, breadcrumb, FAQ) produce valid JSON via json.dumps()
- CSS cascade is correct: tokens -> components -> styles, all referencing --gtme-* variables
- Responsive nav collapses to hamburger at 768px
- Footer has newsletter mini-form HTML (non-functional placeholder for Phase 1)
</success_criteria>

<output>
After completion, create `.planning/phases/01-build-system-and-html-shell/01-01-SUMMARY.md`
</output>
