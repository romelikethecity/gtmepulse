---
phase: 12-quality-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build.py
  - scripts/templates.py
  - content/tools_enrichment.py
  - content/tools_outbound.py
  - content/tools_crm.py
  - content/tools_automation.py
  - content/tools_intent.py
  - content/tools_analytics.py
  - content/tools_linkedin.py
  - content/comparisons_enrichment.py
  - content/comparisons_outbound.py
  - content/comparisons_crm.py
  - content/comparisons_automation.py
  - content/comparisons_intent.py
  - content/comparisons_analytics.py
  - content/comparisons_linkedin.py
  - content/alternatives.py
  - content/roundups.py
autonomous: true
requirements:
  - QUAL3-01
  - QUAL3-02
  - QUAL3-03

must_haves:
  truths:
    - "Every tool review page has SoftwareApplication JSON-LD schema verified by build-time validation"
    - "Every comparison, alternatives, and roundup page has FAQPage schema with 3+ Q&A pairs"
    - "python3 scripts/build.py completes with zero validation warnings across all 263 pages"
  artifacts:
    - path: "scripts/build.py"
      provides: "Enhanced validate_pages() with QUAL3 schema checks"
      contains: "QUAL3-01"
    - path: "output/"
      provides: "263 pages built with zero warnings"
  key_links:
    - from: "scripts/build.py validate_pages()"
      to: "output/tools/*-review/index.html"
      via: "SoftwareApplication schema check"
      pattern: "softwareapplication"
    - from: "scripts/build.py validate_pages()"
      to: "output/tools/*-vs-*/index.html"
      via: "FAQPage schema check"
      pattern: "faqpage"
    - from: "scripts/build.py validate_pages()"
      to: "output/tools/*-alternatives/index.html"
      via: "FAQPage schema check"
      pattern: "faqpage"
---

<objective>
Fix all 97 existing build warnings and add QUAL3 schema validation so the full 263-page build completes with zero warnings. This is the final phase of v3.0.

Purpose: Ship a clean site with valid schema on every page type, verified by automated validation at build time.
Output: Zero-warning build, enhanced validate_pages() with QUAL3 checks.
</objective>

<execution_context>
@/Users/rome/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rome/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@scripts/build.py
@scripts/templates.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add QUAL3 schema validation checks to validate_pages()</name>
  <files>scripts/build.py</files>
  <action>
Add three new validation checks inside `validate_pages()` (after the existing QUAL2 checks, before the duplicate detection post-loop):

1. **QUAL3-01: SoftwareApplication schema on tool review pages.**
   Review pages match pattern `tools/*-review/index.html`. Check that `"softwareapplication"` appears in `html_lower`. If missing:
   `warnings.append(f"QUAL3-01: {rel}: tool review missing SoftwareApplication schema")`

2. **QUAL3-02: FAQPage schema on comparison, alternatives, and roundup pages.**
   - Comparisons: `tools/*-vs-*/index.html` (already checked by QUAL2-06 but only for `comparisons/` prefix which is wrong; actual comparisons live under `tools/`). Remove or update the existing QUAL2-06 check at line ~12056 that checks `rel.startswith("comparisons/")` since comparisons are actually under `tools/`. Replace with a check for `*-vs-*` pattern under `tools/`.
   - Alternatives: `tools/*-alternatives/index.html`
   - Roundups: `tools/best-*/index.html`
   For each, check that `"faqpage"` appears in `html_lower`. If missing:
   `warnings.append(f"QUAL3-02: {rel}: {page_type} page missing FAQPage schema")`

3. **QUAL3-02 (extended): FAQ Q&A count validation.**
   For pages that DO have FAQPage schema, count the `"@type": "Question"` occurrences in the JSON-LD block. If fewer than 3:
   `warnings.append(f"QUAL3-02: {rel}: FAQPage schema has fewer than 3 Q&A pairs")`

Also update the category index exemptions: category index pages (`tools/category/*/index.html`) are listing pages, not data pages. Add them to a `SKIP_WORD_COUNT` set and a `SKIP_SOURCE_CITATION` set so they stop triggering QUAL2-04 and QUAL2-08 warnings. These are index/listing pages with tool cards, not long-form content. They legitimately have lower word counts and no source citations.
  </action>
  <verify>
    <automated>cd /Users/rome/Documents/projects/gtmepulse && python3 -c "
import scripts.build as b
# Just verify validate_pages function has the new checks
import inspect
src = inspect.getsource(b.validate_pages)
assert 'QUAL3-01' in src, 'Missing QUAL3-01 check'
assert 'QUAL3-02' in src, 'Missing QUAL3-02 check'
print('QUAL3 checks present in validate_pages()')
"</automated>
  </verify>
  <done>validate_pages() contains QUAL3-01 and QUAL3-02 checks that detect missing SoftwareApplication schema on review pages and missing/insufficient FAQPage schema on comparison, alternatives, and roundup pages. Category index pages exempted from word count and source citation checks.</done>
</task>

<task type="auto">
  <name>Task 2: Fix all build warnings to achieve zero-warning build</name>
  <files>scripts/build.py, content/tools_enrichment.py, content/tools_outbound.py, content/tools_crm.py, content/tools_automation.py, content/tools_intent.py, content/tools_analytics.py, content/tools_linkedin.py</files>
  <action>
Fix all 97 current warnings across these categories:

**QUAL2-01 title length (60 warnings):**
Every title must be 50-60 characters. The pattern is `"{Tool} Review 2026: {subtitle} - GTME Pulse"`. Most are too long (61-78 chars) or too short (37-49 chars).

For titles that are too long: shorten the subtitle portion. Examples:
- "6sense Review 2026: Enterprise Intent Data at Enterprise Prices - GTME Pulse" (76) -> shorten subtitle
- "PhantomBuster Review 2026: LinkedIn Automation and Its Risks - GTME Pulse" (73) -> shorten subtitle

For titles that are too short: expand the subtitle. Examples:
- "Best Free GTM Tools 2026 - GTME Pulse" (37) -> add descriptor words
- "Best AI Tools for GTM 2026 - GTME Pulse" (39) -> add descriptor words

For glossary titles that are too short (49 chars like "What is Sending Domain? GTM Glossary - GTME Pulse"):
- Adjust the glossary title formula in the `generate_glossary_term()` function to produce titles in the 50-60 char range. Consider adding "for GTM Engineers" or similar qualifier.

For the one glossary description warning (zap, 149 chars): pad to 150+ chars.

Title strings are in the TOOL_REVIEWS list, TOOL_COMPARISONS data, alternatives/roundups data, and glossary generator. Find each title source and adjust.

**QUAL2-04 word count (28 warnings):**
- 8 category index pages (315-415 words): Exempted in Task 1 (SKIP_WORD_COUNT). No content fix needed.
- 18 tool review pages (694-938 words): These reviews need more content in their content modules. For each review below 1000 words, expand the content module's `overview`, `gtm_use_cases`, or `criticism` sections with 100-300 additional words of substantive content following CLAUDE.md writing standards. Target 1,000-1,200 words per review.
- 2 roundup pages (905, 937 words): Expand content to reach 1,000+ words.

Reviews to expand (18): salesloft, zoominfo, cognism, leadiq, clearbit, outreach, fullenrich, heyreach, close, salesforce, woodpecker, lemlist, instantly, persana, attio, lusha, pipedrive, smartlead.
Roundups to expand (2): best-workflow-automation-tools, best-linkedin-prospecting-tools.

**QUAL2-08 source citation (8 warnings):**
All 8 are category index pages. Exempted in Task 1 (SKIP_SOURCE_CITATION). No content fix needed.

**QUAL2-03 internal links (1 warning):**
jobs/index.html has 0 internal links in content. Add 3+ internal links to the job board page content area. Link to relevant pages: /salary/ (salary data), /careers/what-is-a-gtm-engineer/ (role overview), /tools/ (tools index), /glossary/gtm-engineer/ (glossary).

**Writing standards for expanded content:**
- Use contractions, vary sentence length, include specific numbers
- NO em-dashes, NO banned words, NO false reframes
- Every added paragraph must pass the three-test rule (visualize, falsify, unique)
  </action>
  <verify>
    <automated>cd /Users/rome/Documents/projects/gtmepulse && python3 scripts/build.py 2>&1 | grep -c "WARNING:" | xargs -I{} test {} -eq 0 && echo "ZERO WARNINGS" || (python3 scripts/build.py 2>&1 | grep "WARNING:" && echo "FAIL: warnings remain")</automated>
  </verify>
  <done>python3 scripts/build.py completes with zero validation warnings across all 263 pages. Every review has 1000+ words. Every title is 50-60 characters. Jobs page has 3+ internal links. All QUAL3 schema checks pass.</done>
</task>

</tasks>

<verification>
Run complete build and confirm:
1. `python3 scripts/build.py 2>&1 | grep "WARNING:"` returns no output
2. `python3 scripts/build.py 2>&1 | grep "all clear"` shows "Content validation: all clear"
3. Page count remains 263
4. Spot-check 3 review pages have `SoftwareApplication` in source
5. Spot-check 3 comparison pages have `FAQPage` with 3+ Questions in source
</verification>

<success_criteria>
- `python3 scripts/build.py` completes with zero validation warnings
- validate_pages() includes QUAL3-01 (SoftwareApplication on reviews) and QUAL3-02 (FAQPage on comparisons/alternatives/roundups)
- All 263 pages build successfully
- Every review page word count >= 1000
- Every title length 50-60 characters
- v3.0 milestone complete
</success_criteria>

<output>
After completion, create `.planning/phases/12-quality-sweep/12-01-SUMMARY.md`
</output>
