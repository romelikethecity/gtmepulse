---
phase: 11-glossary-job-board-and-newsletter
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - worker/subscribe.js
  - worker/wrangler.toml
  - scripts/generate_weekly_email.py
  - scripts/send_weekly_email.sh
  - scripts/templates.py
  - scripts/nav_config.py
autonomous: false
requirements: [NEWS-01, NEWS-02, NEWS-03, NEWS-04]

user_setup:
  - service: resend
    why: "Email sending and subscriber management"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys (reuse existing key from RevOps Report account)"
      - name: RESEND_AUDIENCE_ID
        source: "Resend Dashboard -> Audiences -> Create new audience 'GTME Pulse' -> copy ID"
    dashboard_config:
      - task: "Verify gtmepulse.com domain in Resend"
        location: "Resend Dashboard -> Domains -> Add Domain -> add DNS records in Cloudflare"
      - task: "Create new Audience for GTME Pulse subscribers"
        location: "Resend Dashboard -> Audiences -> Create Audience"
  - service: cloudflare-workers
    why: "Newsletter signup endpoint"
    env_vars: []
    dashboard_config:
      - task: "Set worker secrets after deploy"
        location: "CLI: cd worker && npx wrangler secret put RESEND_API_KEY && npx wrangler secret put RESEND_AUDIENCE_ID"

must_haves:
  truths:
    - "Newsletter signup forms on the site POST to the Cloudflare Worker endpoint"
    - "Worker validates email and adds contact to Resend Audience"
    - "Weekly email script generates branded HTML email from scraper data with week-over-week diffs"
    - "Cron script runs the email generator every Monday"
  artifacts:
    - path: "worker/subscribe.js"
      provides: "Cloudflare Worker for newsletter signups"
      contains: "RESEND_AUDIENCE_ID"
    - path: "worker/wrangler.toml"
      provides: "Worker deployment config"
      contains: "gtme-newsletter-signup"
    - path: "scripts/generate_weekly_email.py"
      provides: "Weekly email content generator and sender"
      contains: "generate_email_html"
    - path: "scripts/send_weekly_email.sh"
      provides: "Monday cron script"
      contains: "generate_weekly_email.py"
  key_links:
    - from: "scripts/templates.py"
      to: "worker/subscribe.js"
      via: "handleSignup() JS fetch call"
      pattern: "gtme-newsletter-signup"
    - from: "scripts/send_weekly_email.sh"
      to: "scripts/generate_weekly_email.py"
      via: "python3 invocation"
      pattern: "generate_weekly_email"
---

<objective>
Build the newsletter infrastructure: Cloudflare Worker for signups, wire the existing signup forms to the worker, create the weekly email generator that reads scraper data and sends branded emails via Resend, and create the cron script for automated Monday sends.

Purpose: Complete the newsletter pipeline so subscribers get weekly GTM Engineer market intelligence automatically.
Output: Worker files (subscribe.js, wrangler.toml), email generator script, cron script, wired signup forms.
</objective>

<execution_context>
@/Users/rome/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rome/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md (newsletter strategy section, architecture, key files, setup steps, reference implementation)
@scripts/templates.py (get_footer_html with newsletter form, newsletter_cta_html, get_page_wrapper inline JS)
@scripts/nav_config.py (SIGNUP_WORKER_URL not yet defined)

<interfaces>
<!-- Reference implementation from RevOps Report -->

From /Users/rome/Documents/revops_report/worker/subscribe.js:
- Cloudflare Worker handling POST {email} -> Resend Audiences API
- CORS headers, email validation, error handling
- Env vars: RESEND_API_KEY, RESEND_AUDIENCE_ID

From /Users/rome/Documents/revops_report/worker/wrangler.toml:
- name = "revops-newsletter-signup"
- Secrets set via CLI (wrangler secret put)

From /Users/rome/Documents/revops_report/scripts/generate_weekly_email.py:
- Loads market_intelligence.json + comp_analysis.json
- Compares to previous_market_snapshot.json for week-over-week diffs
- Renders branded HTML email
- Sends via Resend API to all subscribers
- --preview and --send flags

From /Users/rome/Documents/revops_report/scripts/send_weekly_email.sh:
- Loads .env, git pulls, runs generator with --send
- Uses scrapers venv on server
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Cloudflare Worker and wire signup forms</name>
  <files>worker/subscribe.js, worker/wrangler.toml, scripts/templates.py, scripts/nav_config.py</files>
  <action>
**1. Create worker/subscribe.js** — Clone from RevOps Report pattern with these changes:
- ALLOWED_ORIGINS: `['https://gtmepulse.com', 'https://www.gtmepulse.com', 'http://localhost:8090']`
- Same Resend Audiences API integration (POST to /audiences/{RESEND_AUDIENCE_ID}/contacts)
- Same CORS, email validation, error handling pattern
- Env vars via wrangler secrets: RESEND_API_KEY, RESEND_AUDIENCE_ID

**2. Create worker/wrangler.toml:**
```toml
name = "gtme-newsletter-signup"
main = "subscribe.js"
compatibility_date = "2024-01-01"
```
With comments about setting secrets via CLI.

**3. Add SIGNUP_WORKER_URL to nav_config.py:**
```python
SIGNUP_WORKER_URL = "https://gtme-newsletter-signup.rome-workers.workers.dev"
```

**4. Wire handleSignup() JS in templates.py:**
In get_page_wrapper(), add a handleSignup() function to the existing inline JS block. This function:
- Prevents default form submit
- Gets email from input
- POSTs JSON {email} to SIGNUP_WORKER_URL
- On success: replaces form with "You're in! Check your inbox." message
- On error: shows error message below button
- Disables button during fetch ("Subscribing...")

Then attach handleSignup to ALL newsletter forms on the page. After the mobile nav and dropdown JS, add:
```javascript
// Newsletter signup
var SIGNUP_URL = '{SIGNUP_WORKER_URL}';
document.querySelectorAll('.hero-signup, .footer-newsletter-form, .newsletter-cta-form').forEach(function(form) {
    form.onsubmit = function(e) {
        e.preventDefault();
        var email = form.querySelector('input[type="email"]').value;
        var btn = form.querySelector('button');
        // ... fetch logic
    };
});
```

Import SIGNUP_WORKER_URL from nav_config in templates.py (it already imports from nav_config).
  </action>
  <verify>
    <automated>cd /Users/rome/Documents/projects/gtmepulse && test -f worker/subscribe.js && test -f worker/wrangler.toml && python3 -c "
from scripts.nav_config import SIGNUP_WORKER_URL
assert 'gtme-newsletter-signup' in SIGNUP_WORKER_URL
print(f'OK: SIGNUP_WORKER_URL = {SIGNUP_WORKER_URL}')
" && python3 scripts/build.py 2>&1 | tail -5 && grep -c 'SIGNUP_URL\|newsletter-signup' output/index.html && echo "OK: signup JS wired"</automated>
  </verify>
  <done>worker/subscribe.js and wrangler.toml exist. SIGNUP_WORKER_URL in nav_config.py. handleSignup JS wired into all pages via templates.py. Build succeeds. All signup forms on every page POST to the worker URL.</done>
</task>

<task type="auto">
  <name>Task 2: Create weekly email generator and cron script</name>
  <files>scripts/generate_weekly_email.py, scripts/send_weekly_email.sh</files>
  <action>
**1. Create scripts/generate_weekly_email.py** — Clone RevOps Report pattern, adapted for GTME Pulse branding:

- **Data loading:** Load data/market_intelligence.json and data/comp_analysis.json (graceful fallback if missing). Load data/previous_market_snapshot.json for week-over-week diffs.
- **Brand constants:** Use GTME Pulse colors from tokens.css: bg #0D0D0D, surface #1A1A1A, amber #E8A838, gold #F5C45C, deep amber #C47A1A, text #E8E8E8. Font: Sora for headings, Plus Jakarta Sans for body (web-safe fallbacks).
- **FROM_EMAIL:** `"The GTME Pulse <insights@gtmepulse.com>"`
- **SITE_URL:** `"https://gtmepulse.com"`

- **Email sections:**
  1. Header with GTME Pulse branding and week date
  2. Market Pulse: total GTM Engineer roles, week-over-week change with trend arrow
  3. Salary Snapshot: median salary, any shifts from last week
  4. Top Hiring Companies: top 5 companies by open GTM roles
  5. Tool Trends: most-mentioned tools in job postings this week
  6. Hiring Signals: notable new postings or trends
  7. Footer with unsubscribe link, site link, "Career intelligence for GTM Engineers"

- **CLI flags:** --preview (write HTML to email_preview.html), --send (send to all Resend audience contacts), --add-subscriber EMAIL, --list-subscribers
- **Resend integration:** Use requests library to call Resend API (same pattern as RevOps Report). Fetch subscriber list from Resend Audiences API. Send individual emails via Resend send endpoint.
- **Snapshot save:** After sending, save current data as previous_market_snapshot.json for next week's diff.

- **Graceful degradation:** If data files are missing or empty, generate email with "No new data this week" placeholder instead of crashing.

**2. Create scripts/send_weekly_email.sh** — Monday cron script:
```bash
#!/bin/bash
# Weekly email send — cron every Monday 10 AM PST (18:00 UTC)
# Cron: 0 18 * * 1 /bin/bash /home/rome/gtmepulse/scripts/send_weekly_email.sh >> /home/rome/logs/gtme_email.log 2>&1
```
- Set -e for fail-fast
- Find project dir relative to script
- Use scrapers venv if on server, else system python3
- Load .env if exists
- git pull --rebase --autostash
- Run: python3 scripts/generate_weekly_email.py --send
- Log timestamps
  </action>
  <verify>
    <automated>cd /Users/rome/Documents/projects/gtmepulse && python3 scripts/generate_weekly_email.py --preview 2>&1 && test -f email_preview.html && echo "OK: preview generated" && bash -n scripts/send_weekly_email.sh && echo "OK: cron script syntax valid"</automated>
  </verify>
  <done>generate_weekly_email.py generates branded HTML email preview from data files. send_weekly_email.sh is syntactically valid and ready for server cron. --preview flag writes email_preview.html. --send flag will send to Resend subscribers when API key is configured.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify newsletter infrastructure</name>
  <files>worker/subscribe.js, scripts/templates.py, scripts/generate_weekly_email.py</files>
  <action>
Human verifies the newsletter infrastructure works correctly before deployment.
  </action>
  <verify>
    <automated>cd /Users/rome/Documents/projects/gtmepulse && test -f worker/subscribe.js && test -f email_preview.html && echo "OK: artifacts present"</automated>
  </verify>
  <done>User has verified: (1) signup forms fire JS on submit, (2) email preview renders with GTME branding, (3) worker code reviewed for correctness.</done>
</task>

</tasks>

<verification>
- worker/subscribe.js exists with Resend Audiences integration and GTME Pulse CORS origins
- worker/wrangler.toml exists with name "gtme-newsletter-signup"
- SIGNUP_WORKER_URL defined in nav_config.py
- handleSignup JS present in every page's inline script (via templates.py)
- All newsletter forms (hero, footer, inline CTA) wired to POST to worker URL
- generate_weekly_email.py generates preview HTML with GTME Pulse branding
- send_weekly_email.sh is a valid bash script ready for server cron
- Build succeeds with no errors
</verification>

<success_criteria>
Newsletter signup forms on every page POST to the Cloudflare Worker. Worker code ready for deployment. Weekly email generator produces branded GTME Pulse emails from scraper data. Cron script ready for server installation.
</success_criteria>

<output>
After completion, create `.planning/phases/11-glossary-job-board-and-newsletter/11-03-SUMMARY.md`
</output>
