---
phase: 11-glossary-job-board-and-newsletter
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build.py
  - scripts/nav_config.py
  - assets/css/styles.css
autonomous: true
requirements: [JOBS-01, JOBS-02]

must_haves:
  truths:
    - "User can visit /jobs/ and see job cards with title, company, location, salary range, and remote badge"
    - "User can filter jobs by seniority, location, and remote status using client-side filters"
    - "Job board displays aggregate stats banner showing total roles, avg salary, percent remote"
    - "Job board reads from data/jobs.json at build time and renders cards statically"
  artifacts:
    - path: "scripts/build.py"
      provides: "Job board page generator"
      contains: "build_job_board"
    - path: "data/jobs.json"
      provides: "Sample job data for build pipeline"
  key_links:
    - from: "scripts/build.py"
      to: "data/jobs.json"
      via: "json.load"
      pattern: "jobs.json"
    - from: "/jobs/ page"
      to: "filter JS"
      via: "inline script"
      pattern: "filterJobs"
---

<objective>
Build the job board page at /jobs/ that reads GTM Engineer job postings from data/jobs.json, displays them as filterable cards with an aggregate stats banner, and integrates into the site nav.

Purpose: Give GTM Engineers a curated job board specific to their role, using data from the unified scraper pipeline (audience id=8, gtme).
Output: 1 new page (/jobs/), sample data file, CSS for job cards, filter JS.
</objective>

<execution_context>
@/Users/rome/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rome/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@scripts/build.py (lines 460-500 for homepage hero pattern, page generator patterns)
@scripts/templates.py (breadcrumb_html, newsletter_cta_html, write_page, get_page_wrapper)
@scripts/nav_config.py
@CLAUDE.md (job board section, display requirements, scraper data format)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sample jobs.json and build job board page generator</name>
  <files>data/jobs.json, scripts/build.py, scripts/nav_config.py, assets/css/styles.css</files>
  <action>
**1. Create data/jobs.json** with sample data matching the unified scraper StandardExporter format. Include 10-15 sample job entries. Each entry:
```json
{
  "title": "Senior GTM Engineer",
  "company": "Clay",
  "location": "San Francisco, CA",
  "salary_min": 150000,
  "salary_max": 200000,
  "salary_display": "$150K-$200K",
  "remote": true,
  "seniority": "Senior",
  "url": "https://example.com/job/123",
  "posted_date": "2026-03-10",
  "source": "LinkedIn"
}
```

Seniority values: "Junior", "Mid", "Senior", "Lead", "Staff", "Director", "VP". Include a mix across seniority levels, some remote/some not, varied locations (SF, NYC, Austin, Remote, London), varied salary ranges. Use realistic company names from the GTM ecosystem (Clay, Apollo, HubSpot, Instantly, Attio, 6sense, Outreach, etc.).

**2. In build.py, add build_job_board():**

Load data/jobs.json (graceful fallback if file missing: show "No current postings" message).

Generate /jobs/index.html with:
- BreadcrumbList schema (Home > Jobs)
- h1: "GTM Engineer Jobs"
- Lead paragraph: "Curated GTM Engineering roles from across the industry. Updated from 3,000+ tracked B2B SaaS job postings."

**Aggregate stats banner** (computed from the JSON data):
- Total open roles
- Median salary (from jobs with salary data)
- Percent remote
- Top hiring city
Use the stats-grid CSS pattern already used on salary index (3-4 stat cards in a row).

**Filter bar** (client-side JS, no page reload):
- Seniority dropdown: All / Junior / Mid / Senior / Lead / Staff / Director / VP
- Location text input with datalist of unique locations
- Remote toggle checkbox
All filters applied via inline JS that shows/hides job cards by toggling a CSS class. Keep the JS minimal and inline in the page.

**Job cards**: Each card shows:
- Title (h3, linked to job URL, opens in new tab)
- Company name
- Location + remote badge (if remote: green "Remote" tag)
- Salary range (if available, else "Salary not disclosed")
- Seniority tag
- Posted date (relative: "3 days ago", "1 week ago")
- Source badge

Use a card grid layout (2 cols on desktop, 1 on mobile). Each card gets data attributes for filtering: `data-seniority`, `data-location`, `data-remote`.

- newsletter_cta_html("jobs") at bottom
- Add to ALL_PAGES

**3. CSS in styles.css**: Add job-board-specific styles:
- `.job-card` — card with surface background, border-radius 16px, padding, hover shadow (match existing card pattern)
- `.job-card-title` — amber link color
- `.job-card-meta` — secondary text for company, location
- `.job-badge-remote` — small green badge
- `.job-badge-seniority` — small amber/gold badge
- `.job-salary` — Source Code Pro font for salary numbers
- `.job-filters` — filter bar with inline controls
- `.job-stats-banner` — stats row (reuse existing stats-grid if possible)

**4. nav_config.py**: Add /jobs/ to NAV_ITEMS (as sibling after "Careers") and to FOOTER_COLUMNS under "Resources".

**5. main()**: Add build_job_board() call before build_sitemap().

Title: "GTM Engineer Jobs: Open Roles and Hiring Data"
Meta description: "Browse curated GTM Engineer job postings with salary data, remote filters, and hiring trends. Updated from 3,000+ tracked B2B SaaS positions."
  </action>
  <verify>
    <automated>cd /Users/rome/Documents/projects/gtmepulse && python3 scripts/build.py 2>&1 | tail -10 && test -f output/jobs/index.html && echo "OK: jobs page built" && python3 -c "import json; d=json.load(open('data/jobs.json')); print(f'OK: {len(d)} jobs in data file')"</automated>
  </verify>
  <done>Build completes with jobs page. /jobs/index.html shows job cards with stats banner. data/jobs.json has 10-15 sample entries. Filter bar dropdowns present in HTML. Nav and footer include Jobs link. CSS cache version incremented.</done>
</task>

</tasks>

<verification>
- `python3 scripts/build.py` produces the jobs page with no errors
- /jobs/index.html exists with job cards, stats banner, and filter controls
- data/jobs.json exists with valid JSON array of job objects
- Job cards show title, company, location, salary, seniority, remote badge
- Filter data attributes present on cards (data-seniority, data-location, data-remote)
- Inline JS for filtering is present and syntactically valid
- Nav includes Jobs link
</verification>

<success_criteria>
User can visit /jobs/ and see job cards with title, company, location, salary range, remote badge, and seniority tags. Stats banner shows aggregate data. Filter controls are present for seniority, location, and remote status.
</success_criteria>

<output>
After completion, create `.planning/phases/11-glossary-job-board-and-newsletter/11-02-SUMMARY.md`
</output>
