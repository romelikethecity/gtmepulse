---
phase: 06-tools-and-benchmarks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [scripts/build.py]
autonomous: true
requirements: [TOOL-01, TOOL-02, TOOL-03, TOOL-04, TOOL-05]

must_haves:
  truths:
    - "Visitor can browse a tools index page showing all GTM Engineer tool categories with adoption rates"
    - "Clay deep-dive page shows 84% adoption, 96% agencies, most loved AND most frustrating data"
    - "CRM page shows 92% adoption with Salesforce vs HubSpot split"
    - "AI coding tools page shows 71% adoption with Cursor and Claude Code data"
    - "n8n page shows 54% adoption with agency vs in-house usage patterns"
  artifacts:
    - path: "output/tools/index.html"
      provides: "Tools index with category cards and adoption overview"
    - path: "output/tools/clay/index.html"
      provides: "Clay adoption deep-dive"
    - path: "output/tools/crm-adoption/index.html"
      provides: "CRM adoption analysis"
    - path: "output/tools/ai-coding-tools/index.html"
      provides: "AI coding tools adoption"
    - path: "output/tools/n8n-adoption/index.html"
      provides: "n8n adoption analysis"
    - path: "output/tools/tech-stack-benchmark/index.html"
      provides: "Full tech stack benchmark page"
  key_links:
    - from: "output/tools/index.html"
      to: "all tool pages"
      via: "TOOL_PAGES array links"
      pattern: "/tools/"
    - from: "tool pages"
      to: "/salary/ pages"
      via: "tool_related_links()"
      pattern: "salary.*coding-premium"
---

<objective>
Build the tools index page and first 5 tool pages covering the core GTM Engineer tech stack: tech stack benchmark overview, Clay deep-dive, CRM adoption, AI coding tools, and n8n adoption.

Purpose: These are the anchor pages for the tools section. The tech stack benchmark is the category-level overview, and Clay/CRM/AI/n8n are the highest-adoption tools with the most survey data to back them.

Output: 6 new pages under /tools/, TOOL_PAGES array, tool_related_links() helper function.
</objective>

<execution_context>
@/Users/rome/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rome/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@scripts/build.py
@scripts/templates.py
@scripts/nav_config.py

<interfaces>
<!-- Established patterns from Phase 4-5 that executor MUST follow -->

Page array pattern (from CAREER_PAGES, AGENCY_PAGES, JOBMKT_PAGES):
```python
TOOL_PAGES = [
    {"slug": "tech-stack-benchmark", "title": "Tech Stack Benchmark"},
    {"slug": "clay", "title": "Clay: 84% Adoption"},
    ...
]
```

Cross-link helper pattern (from career_related_links, agency_related_links, jobmkt_related_links):
```python
def tool_related_links(current_slug):
    """Generate related tool page links."""
    links = [("/tools/", "Tools Index")]
    for page in TOOL_PAGES:
        if page["slug"] != current_slug:
            links.append((f"/tools/{page['slug']}/", page["title"]))
    # Add salary cross-links
    links.append(("/salary/coding-premium/", "Coding Premium: $45K Gap"))
    return links[:12]
```

Page generator pattern (from build_career_*, build_agency_*):
```python
def build_tool_clay():
    slug = "clay"
    path = f"tools/{slug}"
    title = "Clay for GTM Engineers: 84% Adoption (2026)"
    description = "..."  # 150-158 chars
    crumbs = [("Home", "/"), ("Tools", "/tools/"), ("Clay", None)]
    # ... stats grid, content sections, FAQ, related links, citation, newsletter CTA
```

Required template functions: write_page(), breadcrumb_html(), breadcrumb_schema(), faq_schema_and_html(), source_citation_html(), newsletter_cta_html()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tools index + TOOL_PAGES array + tool_related_links() + tech stack benchmark and Clay pages</name>
  <files>scripts/build.py</files>
  <action>
Add to scripts/build.py (following the CAREER_PAGES/AGENCY_PAGES/JOBMKT_PAGES pattern):

1. Create TOOL_PAGES array with all 16 tool page entries (slugs and titles for the full section, even though only 5 are built in this plan). Place it above the first tool build function.

2. Create tool_related_links(current_slug) helper that cross-links tool pages plus salary coding premium and salary index. Limit to 12 links. Follow the exact pattern of agency_related_links().

3. Build tools index page (build_tool_index):
   - Path: tools/index.html
   - Title: "GTM Engineer Tools: Tech Stack Data (2026)" (50-60 chars)
   - Stats grid showing: Clay 84%, CRM 92%, AI Coding 71%, n8n 54%
   - Grid of cards linking to each tool page in TOOL_PAGES (use salary-index-card pattern from career index)
   - Only link to pages that exist (the 5 being built now); show the rest as "Coming soon" cards
   - BreadcrumbList schema, source citation, newsletter CTA
   - 1,200+ words with overview of GTM Engineer tooling landscape

4. Build tech stack benchmark page (build_tool_tech_stack):
   - Path: tools/tech-stack-benchmark/index.html
   - Title: "GTM Engineer Tech Stack: 2026 Benchmark" (50-60 chars)
   - Full adoption data: Clay 84% (96% agencies), CRM 92%, AI coding 71%, n8n 54%, Unify 8.8%, ZoomInfo/Apollo 65%
   - Agency vs in-house splits for each category
   - Annual spend data: 55% of agencies spend $5-25K on tools
   - Stats grid, content sections covering each tool category
   - BreadcrumbList + FAQPage schema (3-4 Q&A about GTM tool stacks)
   - tool_related_links(), source_citation_html(), newsletter CTA
   - 1,500+ words

5. Build Clay deep-dive page (build_tool_clay):
   - Path: tools/clay/index.html
   - Title: "Clay for GTM Engineers: 84% Adoption (2026)" (50-60 chars)
   - Stats: 84% overall adoption, 96% agencies, most loved AND most frustrating tool
   - Content: what Clay does, why adoption is so high, agency dependency, frustration data (integration issues, learning curve, UX complaints), salary impact (tie to coding premium)
   - Cross-link to /salary/coding-premium/ and /careers/skills-gap/
   - BreadcrumbList + FAQPage schema, tool_related_links(), source citation
   - 1,200-2,000 words with honest criticism per CLAUDE.md writing standards

Add build_tool_index(), build_tool_tech_stack(), build_tool_clay() calls to main() under a new "Building tool pages..." print section.

Run `python3 scripts/build.py` and verify zero validation warnings.
  </action>
  <verify>
    <automated>cd /Users/rome/Documents/projects/gtmepulse && python3 scripts/build.py 2>&1 | tail -20</automated>
  </verify>
  <done>Tools index, tech stack benchmark, and Clay pages render with 1,200+ words each, BreadcrumbList/FAQPage schema, source citations, tool_related_links cross-links. Build produces zero validation warnings.</done>
</task>

<task type="auto">
  <name>Task 2: CRM adoption, AI coding tools, and n8n adoption pages</name>
  <files>scripts/build.py</files>
  <action>
Add 3 more tool page generators to scripts/build.py:

1. Build CRM adoption page (build_tool_crm):
   - Path: tools/crm-adoption/index.html
   - Title: "CRM Adoption for GTM Engineers: 92% (2026)" (50-60 chars)
   - Stats: 92% CRM adoption, Salesforce vs HubSpot split, company size preferences
   - Content: why CRM is near-universal for GTMEs, HubSpot vs Salesforce breakdown by company size, integration with Clay/Apollo, CRM as data layer vs pipeline tool, frustrations (data hygiene, permission models)
   - Cross-link to /salary/company-size/ and /tools/clay/
   - BreadcrumbList + FAQPage schema, tool_related_links(), source citation
   - 1,200-2,000 words

2. Build AI coding tools page (build_tool_ai_coding):
   - Path: tools/ai-coding-tools/index.html
   - Title: "AI Coding Tools for GTM Engineers: 71% Use Them" (50-60 chars)
   - Stats: 71% adoption, Cursor and Claude Code as leaders, coding premium tie-in ($45K)
   - Content: which AI coding tools GTMEs use, Cursor vs Claude Code vs ChatGPT for code gen, how AI tools enable non-coders to bridge the gap, salary impact of AI-assisted coding, limitations and hallucination concerns
   - Cross-link to /salary/coding-premium/ and /careers/do-you-need-to-code/
   - BreadcrumbList + FAQPage schema, tool_related_links(), source citation
   - 1,200-2,000 words

3. Build n8n adoption page (build_tool_n8n):
   - Path: tools/n8n-adoption/index.html
   - Title: "n8n for GTM Engineers: 54% Adoption (2026)" (50-60 chars)
   - Stats: 54% adoption, agency vs in-house usage difference, replacing Zapier/Make
   - Content: why n8n is gaining ground (self-hosted, no per-task pricing), agency preference for n8n over Zapier, workflow complexity GTMEs build, integration with Clay/CRM/sequencing tools, learning curve
   - Cross-link to /tools/tech-stack-benchmark/ and /careers/start-gtm-engineering-agency/
   - BreadcrumbList + FAQPage schema, tool_related_links(), source citation
   - 1,200-2,000 words

Add build_tool_crm(), build_tool_ai_coding(), build_tool_n8n() calls to main() in the tool pages section.

Update tools index to link all 5 built pages (not "Coming soon" for these).

Run `python3 scripts/build.py` and verify zero validation warnings.
  </action>
  <verify>
    <automated>cd /Users/rome/Documents/projects/gtmepulse && python3 scripts/build.py 2>&1 | tail -20</automated>
  </verify>
  <done>CRM, AI coding, and n8n pages render with 1,200+ words each, BreadcrumbList/FAQPage schema, source citations, tool_related_links cross-links. All 5 tool pages + index accessible from tools index. Build produces zero validation warnings.</done>
</task>

</tasks>

<verification>
- `python3 scripts/build.py` completes with zero validation warnings
- 6 new pages exist under output/tools/
- Each tool page has unique title (50-60 chars), unique meta description (150-158 chars)
- Each tool page has BreadcrumbList + FAQPage JSON-LD schema
- Each tool page has 1,200+ words, source citation, 3+ internal links
- tool_related_links() cross-links all tool pages plus salary pages
- Tools index shows adoption overview with cards linking to all built pages
</verification>

<success_criteria>
- Tools index and 5 tool pages render correctly at /tools/
- Every page cites "Source: State of GTM Engineering Report 2026 (n=228)"
- No banned words, no em-dashes, no false reframes in content
- Build total page count increases by 6
</success_criteria>

<output>
After completion, create `.planning/phases/06-tools-and-benchmarks/06-01-SUMMARY.md`
</output>
