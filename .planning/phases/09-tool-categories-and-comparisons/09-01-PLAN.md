---
phase: 09-tool-categories-and-comparisons
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build.py
  - scripts/nav_config.py
  - content/comparisons_enrichment.py
  - content/comparisons_outbound.py
  - content/comparisons_crm.py
autonomous: true
requirements: [TCAT-01, TCAT-02, TCAT-03, TCAT-04, TCAT-05, TCAT-06, TCAT-07, TCAT-08, TCMP-01, TCMP-02, TCMP-03, TCMP-04, TCMP-05, TCMP-06, TCMP-07, TCMP-08, TCMP-09, TCMP-10]

must_haves:
  truths:
    - "User can visit 8 category index pages listing tools with summary cards and links to reviews"
    - "User can read 10 comparison pages with feature tables, pricing, verdict, and FAQ"
    - "Every comparison page has FAQPage JSON-LD schema with 3+ Q&A matching visible FAQ"
    - "Category and comparison pages have BreadcrumbList schema"
    - "Nav dropdown includes category pages link"
  artifacts:
    - path: "scripts/build.py"
      provides: "TOOL_CATEGORIES data, generate_category_index(), TOOL_COMPARISONS data, generate_tool_comparison(), build functions"
      contains: "TOOL_CATEGORIES"
    - path: "content/comparisons_enrichment.py"
      provides: "Comparison prose for enrichment category matchups"
      exports: ["COMPARISONS"]
    - path: "content/comparisons_outbound.py"
      provides: "Comparison prose for outbound/sequencing matchups"
      exports: ["COMPARISONS"]
    - path: "content/comparisons_crm.py"
      provides: "Comparison prose for CRM and automation matchups"
      exports: ["COMPARISONS"]
  key_links:
    - from: "scripts/build.py"
      to: "content/comparisons_*.py"
      via: "_load_comparison_content()"
      pattern: "_load_comparison_content"
    - from: "category index pages"
      to: "individual review pages"
      via: "card links to /tools/[tool]-review/"
      pattern: "/tools/.*-review/"
    - from: "comparison pages"
      to: "individual review pages"
      via: "related links section"
      pattern: "review_related_links\\|comparison_related_links"
---

<objective>
Build 8 tool category index pages and the first 10 tool-vs-tool comparison pages with content modules.

Purpose: Category indexes let users browse tools by category (enrichment, outbound, CRM, etc.) with summary cards linking to reviews. Comparison pages provide 3,000-5,000 word head-to-head analyses with feature tables, pricing, and FAQPage schema for SEO.

Output: 8 category pages at /tools/category/[slug]/, 10 comparison pages at /tools/[tool-a]-vs-[tool-b]/, content modules for comparison prose, nav updates.
</objective>

<execution_context>
@/Users/rome/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rome/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-tool-reviews/08-01-SUMMARY.md
@.planning/phases/08-tool-reviews/08-02-SUMMARY.md

<interfaces>
<!-- Key structures executor needs from build.py -->

TOOL_REVIEWS list (line ~5710): Each entry has slug, name, category, url, price_range, title, meta_desc, content_module, content_key

Categories in TOOL_REVIEWS:
- "Data Enrichment & Orchestration" (9 tools: Clay, Apollo, ZoomInfo, Clearbit, FullEnrich, Lusha, Cognism, LeadIQ, Persana)
- "Outbound Sequencing" (7 tools: Instantly, Smartlead, Outreach, Salesloft, Lemlist, HeyReach, Woodpecker)
- "CRM" (5 tools: HubSpot, Salesforce, Pipedrive, Close, Attio)
- "Workflow Automation" (3 tools: Make, n8n, Zapier)
- "Intent Data" (2 tools: 6sense, Bombora)
- "Analytics" (2 tools: Segment, PostHog)
- "LinkedIn & Social" (2 tools: Sales Navigator, PhantomBuster)

From templates.py:
- get_faq_schema(qa_pairs) - FAQPage JSON-LD (line 266)
- faq_html(qa_pairs) - visible FAQ section (line 336)
- get_breadcrumb_schema(crumbs) - BreadcrumbList JSON-LD
- breadcrumb_html(crumbs) - visible breadcrumbs
- get_page_wrapper() - full page assembly
- write_page() - write to disk + track in ALL_PAGES
- newsletter_cta_html() - newsletter CTA block

From build.py:
- _load_review_content(module_name, content_key) (line 5883) - content module loader pattern
- review_related_links(slug) (line 5899) - related links for review pages
- source_citation_html() - source citations
- pad_description() - meta description padding
- generate_tool_review(review) (line 5920) - review page generator pattern to follow
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build 8 category index pages and category infrastructure</name>
  <files>scripts/build.py, scripts/nav_config.py</files>
  <action>
Add TOOL_CATEGORIES data structure and category index page generator to build.py. Follow the pattern from generate_tool_review() and _build_review_index_cards().

1. Add TOOL_CATEGORIES list after TOOL_REVIEWS (~line 5870). Each entry:
   ```python
   {"slug": "data-enrichment", "name": "Data Enrichment & Orchestration", "category_key": "Data Enrichment & Orchestration",
    "title": "Best Data Enrichment Tools for GTM Engineers (2026)",
    "meta_desc": "Compare 9 data enrichment and orchestration tools for GTM Engineers. Clay, Apollo, ZoomInfo, and more with honest reviews and pricing.",
    "intro": "Data enrichment is the foundation of every GTM stack...",  # 2-3 paragraph category overview
    "tools_in_category": ["clay", "apollo", "zoominfo", "clearbit", "fullenrich", "lusha", "cognism", "leadiq", "persana"]}
   ```

   Categories to create (8 total, mapping to CLAUDE.md categories):
   - data-enrichment -> "Data Enrichment & Orchestration" (9 tools)
   - outbound-sequencing -> "Outbound Sequencing" (7 tools)
   - crm -> "CRM" (5 tools)
   - workflow-automation -> "Workflow Automation" (3 tools)
   - ai-llm-tools -> "AI & LLM Tools" (0 reviewed tools, mention Claude/ChatGPT/Perplexity as coming soon)
   - intent-data -> "Intent & Signal Data" (2 tools: 6sense, Bombora)
   - analytics -> "Analytics & Product Signals" (2 tools: Segment, PostHog)
   - linkedin-social -> "LinkedIn & Social" (2 tools: Sales Navigator, PhantomBuster)

   NOTE: Category names in TOOL_CATEGORIES must match the "category" field in TOOL_REVIEWS exactly for the lookup to work. Intent Data maps to "Intent Data" in reviews, Analytics maps to "Analytics", LinkedIn maps to "LinkedIn & Social".

2. Add generate_category_index(cat) function:
   - Breadcrumbs: Home > Tools > [Category Name]
   - BreadcrumbList JSON-LD schema
   - Hero section using salary-header classes with category name as h1
   - Category intro paragraphs (from cat["intro"])
   - Tool cards grid: for each tool slug in tools_in_category, look up the matching TOOL_REVIEWS entry, render a card with tool name, price range, first line of meta_desc, and link to /tools/[slug]/
   - If category has 0 reviewed tools (AI & LLM), show "Reviews coming soon" message instead of cards
   - Related links section at bottom linking to other category pages and the tools index
   - Newsletter CTA
   - Output path: tools/category/[slug]/index.html

3. Add build_tool_categories() function that loops TOOL_CATEGORIES and calls generate_category_index().

4. Add build_tool_categories() call in main() after build_tool_reviews() line (~11352).

5. Update nav_config.py: Add "Tool Categories" entry to the Tools dropdown children list, href="/tools/category/data-enrichment/" (link to first/main category). Also add to FOOTER_COLUMNS under the tools section.

Each category intro should be 2-3 paragraphs (150-300 words), written in the project voice (direct, practitioner-first, no banned words). The intro explains what the category covers, why GTM Engineers care, and what to look for when choosing tools in this category.

Do NOT create separate content modules for category intros. They are short enough to inline in the TOOL_CATEGORIES data structure in build.py.
  </action>
  <verify>
    <automated>cd /Users/rome/Documents/projects/gtmepulse && python3 scripts/build.py 2>&1 | tail -20</automated>
  </verify>
  <done>8 category index pages built at /tools/category/[slug]/index.html. Each lists tools with cards linking to reviews. BreadcrumbList schema on every page. Nav updated with categories link.</done>
</task>

<task type="auto">
  <name>Task 2: Build first 10 comparison pages with content modules</name>
  <files>scripts/build.py, content/comparisons_enrichment.py, content/comparisons_outbound.py, content/comparisons_crm.py</files>
  <action>
Create comparison page generator and content modules for the first 10 tool-vs-tool comparisons.

1. Create content/comparisons_enrichment.py with COMPARISONS dict:
   Keys: "clay-vs-apollo", "clay-vs-zoominfo", "apollo-vs-zoominfo", "clay-vs-clearbit", "cognism-vs-zoominfo", "leadiq-vs-lusha"
   Each value is a dict with sections:
   - "intro": 2-3 paragraph overview of the matchup (why these two get compared)
   - "feature_table": HTML table string comparing 8-12 features (rows) across the two tools (columns). Use checkmarks, X marks, and brief notes. Include features like: data coverage, pricing model, API quality, enrichment depth, CRM integrations, intent signals, free tier, GTM Engineer fit.
   - "tool_a_strengths": 3-5 paragraphs on tool A's advantages
   - "tool_b_strengths": 3-5 paragraphs on tool B's advantages
   - "pricing_comparison": 2-3 paragraphs comparing pricing models, tiers, and total cost of ownership
   - "verdict": 2-3 paragraphs with clear recommendation (who should pick which tool and why)
   - "faq": list of 4-5 (question, answer) tuples

2. Create content/comparisons_outbound.py with COMPARISONS dict:
   Keys: "instantly-vs-smartlead", "outreach-vs-salesloft", "lemlist-vs-instantly", "smartlead-vs-lemlist"
   Same structure as above but for outbound/sequencing tools. Feature table rows: deliverability, warmup, personalization, multi-channel, pricing, analytics, API, GTM Engineer fit.

3. Create content/comparisons_crm.py with COMPARISONS dict:
   Keys: (none from first 10 -- but will be needed for Plan 02)
   Actually, from first 10 comparisons: "hubspot-vs-salesforce" and "make-vs-n8n" and "make-vs-zapier" belong here.
   Keys: "hubspot-vs-salesforce"
   And create content/comparisons_automation.py with: "make-vs-n8n", "make-vs-zapier"

   Correction on file list -- create these content modules:
   - content/comparisons_enrichment.py: clay-vs-apollo (TCMP-01), clay-vs-zoominfo (TCMP-02), apollo-vs-zoominfo (TCMP-08), clay-vs-clearbit (TCMP-10), lemlist-vs-instantly (TCMP-09)
   - content/comparisons_outbound.py: instantly-vs-smartlead (TCMP-03), outreach-vs-salesloft (TCMP-04)
   - content/comparisons_crm.py: hubspot-vs-salesforce (TCMP-05)
   - content/comparisons_automation.py: make-vs-n8n (TCMP-06), make-vs-zapier (TCMP-07)

4. Add TOOL_COMPARISONS list in build.py after TOOL_CATEGORIES. Each entry:
   ```python
   {"slug": "clay-vs-apollo", "tool_a": "Clay", "tool_b": "Apollo.io",
    "title": "Clay vs Apollo 2026: Which GTM Data Platform Wins?",
    "meta_desc": "Clay vs Apollo head-to-head comparison. Feature tables, pricing breakdown, and a clear verdict for GTM Engineers.",
    "content_module": "comparisons_enrichment", "content_key": "clay-vs-apollo",
    "category": "Data Enrichment & Orchestration"}
   ```

   First 10 comparisons (matching TCMP-01 through TCMP-10):
   - TCMP-01: clay-vs-apollo
   - TCMP-02: clay-vs-zoominfo
   - TCMP-03: instantly-vs-smartlead
   - TCMP-04: outreach-vs-salesloft
   - TCMP-05: hubspot-vs-salesforce
   - TCMP-06: make-vs-n8n
   - TCMP-07: make-vs-zapier
   - TCMP-08: apollo-vs-zoominfo
   - TCMP-09: lemlist-vs-instantly
   - TCMP-10: clay-vs-clearbit

5. Add _load_comparison_content(module_name, content_key) function following _load_review_content() pattern (importlib.util.spec_from_file_location). Returns dict from module's COMPARISONS[content_key].

6. Add generate_tool_comparison(comp) function:
   - Breadcrumbs: Home > Tools > [Tool A] vs [Tool B]
   - BreadcrumbList + FAQPage JSON-LD schemas
   - Hero section (salary-header classes) with h1 "[Tool A] vs [Tool B]"
   - Sections: Intro > Feature Comparison (table) > [Tool A] Strengths > [Tool B] Strengths > Pricing > Verdict > FAQ
   - FAQ section using faq_html() for visible content and get_faq_schema() for JSON-LD (must have 3+ Q&A)
   - Related links: link to both tools' review pages, other comparisons in same category, category index
   - Newsletter CTA with comparison-specific hook
   - Output path: tools/[slug]/index.html (e.g., tools/clay-vs-apollo/index.html)

7. Add BUILT_COMPARISON_SLUGS set (same pattern as BUILT_REVIEW_SLUGS).

8. Add build_tool_comparisons() that loops TOOL_COMPARISONS and calls generate_tool_comparison().

9. Add build_tool_comparisons() call in main() after build_tool_categories().

Each comparison must be 3,000-5,000 words total. Write in project voice: direct, opinionated, practitioner-first. Give a clear winner recommendation in the verdict. No hedge words. Include honest criticism of both tools. Follow all CLAUDE.md writing rules (no banned words, no em-dashes, no false reframes).

Feature tables should use existing table CSS from components.css. Comparison page layout reuses salary-header and salary-content CSS classes (same as reviews).
  </action>
  <verify>
    <automated>cd /Users/rome/Documents/projects/gtmepulse && python3 scripts/build.py 2>&1 | tail -20</automated>
  </verify>
  <done>10 comparison pages built at /tools/[slug]/index.html. Each has feature table, pricing comparison, verdict with clear recommendation, FAQ with 4-5 Q&A pairs, FAQPage + BreadcrumbList JSON-LD schema. Content modules contain 3,000-5,000 words per comparison.</done>
</task>

</tasks>

<verification>
- `python3 scripts/build.py` completes without errors
- 8 category pages exist in output/tools/category/*/index.html
- 10 comparison pages exist in output/tools/*/index.html (clay-vs-apollo, etc.)
- Each comparison page source contains `"@type":"FAQPage"` JSON-LD
- Each category and comparison page source contains `"@type":"BreadcrumbList"` JSON-LD
- Nav dropdown includes categories link
- Total page count increases by 18 (8 categories + 10 comparisons)
</verification>

<success_criteria>
- 8 category index pages live with tool cards linking to reviews
- 10 comparison pages live with feature tables, pricing, verdict, FAQ
- Every comparison has FAQPage schema with 3+ Q&A
- All pages have BreadcrumbList schema
- Build runs clean (validation warnings acceptable for title length)
</success_criteria>

<output>
After completion, create `.planning/phases/09-tool-categories-and-comparisons/09-01-SUMMARY.md`
</output>
