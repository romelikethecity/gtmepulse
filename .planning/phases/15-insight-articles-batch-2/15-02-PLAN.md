---
phase: 15-insight-articles-batch-2
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified: [scripts/build.py]
autonomous: true
requirements: [ART-15, ART-16, ART-17]

must_haves:
  truths:
    - "Intent data buying guide accessible at /insights/intent-data-guide/"
    - "CRM hygiene playbook accessible at /insights/crm-hygiene/"
    - "Pulse report template accessible at /insights/pulse-report-template/ with live data from jobs.json"
    - "Insights index page lists all 17 articles"
  artifacts:
    - path: "output/insights/intent-data-guide/index.html"
      provides: "ART-15 intent data buying guide"
    - path: "output/insights/crm-hygiene/index.html"
      provides: "ART-16 CRM hygiene automation playbook"
    - path: "output/insights/pulse-report-template/index.html"
      provides: "ART-17 monthly pulse report template with data injection"
  key_links:
    - from: "scripts/build.py (build_insight_pulse_report)"
      to: "data/jobs.json"
      via: "os.path.join(DATA_DIR, 'jobs.json') with os.path.exists fallback"
      pattern: "jobs\\.json"
---

<objective>
Write 3 insight articles: intent data buying guide, CRM hygiene automation playbook, and the data-driven monthly pulse report template (ART-17 loads jobs.json at build time).

Purpose: Deliver the intent/CRM tactical guides and the only data-driven article in Phase 15 (ART-17), which pulls live stats from the scraper pipeline.
Output: 3 new article pages, insights index showing 17 total articles, zero-warning build.
</objective>

<execution_context>
@/Users/rome/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rome/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-insight-articles-batch-2/15-RESEARCH.md
@.planning/phases/15-insight-articles-batch-2/15-01-SUMMARY.md

@scripts/build.py (INSIGHT_PAGES, BUILT_INSIGHT_SLUGS, article function pattern, line 13544 jobs.json loading pattern)
@data/jobs.json (job data structure for ART-17)
@CLAUDE.md (writing standards, banned words, SEO requirements)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ART-15 (intent data guide) and ART-16 (CRM hygiene)</name>
  <files>scripts/build.py</files>
  <action>
1. Write build_insight_intent_data() for ART-15. Topic: Intent data buying guide for GTM Engineers. Cover: what intent data actually measures (topic consumption, web research spikes, review site activity), comparison of signal types (first-party vs third-party intent), vendor comparison (6sense surge data vs Bombora company surge vs G2 buyer intent vs TrustRadius), integration patterns (CRM enrichment, scoring models, trigger-based outbound), pricing tiers and ROI calculation, when intent data is worth it vs when it's noise. 1,500-2,500 words. Internal links to: /tools/category/intent-signal-data/, /comparisons/6sense-vs-bombora/, /tools/, plus 1+ more. Outbound citations: 6sense buyer intent research, Bombora company surge methodology (2+ links with target="_blank" rel="noopener").

2. Write build_insight_crm_hygiene() for ART-16. Topic: CRM hygiene automation playbook. Cover: the cost of dirty CRM data (pipeline velocity, reporting accuracy, routing errors), automated dedup strategies (fuzzy matching on company name + domain), lifecycle stage automation, lead routing via code vs manual assignment, contact enrichment triggers, dead lead detection and archival, implementation with HubSpot workflows vs Salesforce Apex vs Make/n8n. 1,500-2,500 words. Internal links to: /tools/category/crm/, /comparisons/hubspot-vs-salesforce/, /glossary/, plus 1+ more. Outbound citations: Salesforce data quality reports, HubSpot CRM best practices (2+ links).

3. Uncomment dispatch calls for both functions in main().

Follow all CLAUDE.md writing rules. No em-dashes, no banned words, no false reframes.
  </action>
  <verify>
    <automated>cd /Users/rome/Documents/projects/gtmepulse && python3 scripts/build.py 2>&1 | tail -30</automated>
  </verify>
  <done>Articles at /insights/intent-data-guide/ and /insights/crm-hygiene/ pass validation. BUILT_INSIGHT_SLUGS has 16 slugs. Build completes with zero new warnings.</done>
</task>

<task type="auto">
  <name>Task 2: Write ART-17 (pulse report template with live data)</name>
  <files>scripts/build.py</files>
  <action>
1. Write build_insight_pulse_report() for ART-17. This is the ONE article that loads data at build time. Follow the pattern from build_job_board() at line 13544:

```python
jobs_path = os.path.join(DATA_DIR, "jobs.json")
jobs = []
if os.path.exists(jobs_path):
    with open(jobs_path, "r", encoding="utf-8") as f:
        jobs = json.load(f)
```

2. Compute aggregate stats from jobs data: total roles, remote percentage, salary range (min/max from salary_min/salary_max fields), top hiring companies, seniority distribution. Use os.path.exists() with sensible fallbacks if data is thin (the file currently has 7 entries).

3. Article topic: Monthly GTM pulse report template. Cover: what a monthly pulse report should contain (market snapshot, salary movement, tool adoption shifts, hiring velocity), how to build one from scraper data, template sections with the LIVE computed stats rendered inline, methodology for week-over-week and month-over-month comparisons, how to present findings to leadership. Frame this as both a template the reader can copy AND a live example using current data. 1,500-2,500 words.

4. Stat cards should show live computed values (total roles, remote %, median salary if available, top company). Add a "Data as of: {date}" note.

5. Internal links to: /insights/, /salary/, /jobs/, /insights/job-market-2026/, plus 1+ more. Outbound citations: BLS employment data, LinkedIn Economic Graph (2+ links).

6. Uncomment dispatch call in main().

Follow all CLAUDE.md writing rules.
  </action>
  <verify>
    <automated>cd /Users/rome/Documents/projects/gtmepulse && python3 scripts/build.py 2>&1 | tail -30</automated>
  </verify>
  <done>Article at /insights/pulse-report-template/ renders with live data from jobs.json. Stat cards show computed values (or sensible fallbacks). BUILT_INSIGHT_SLUGS has 17 slugs. Insights index shows 17 articles. Build completes with zero new warnings.</done>
</task>

</tasks>

<verification>
- `python3 scripts/build.py` completes with zero warnings
- 3 new article pages exist in output/insights/
- ART-17 article loads data/jobs.json and renders computed stats
- Each article has Article JSON-LD, 3+ internal links, 2+ outbound citations
- Insights index shows 17 total articles
</verification>

<success_criteria>
3 insight articles (ART-15, ART-16, ART-17) live and passing validation. ART-17 renders live data from jobs.json. Zero-warning build.
</success_criteria>

<output>
After completion, create `.planning/phases/15-insight-articles-batch-2/15-02-SUMMARY.md`
</output>
