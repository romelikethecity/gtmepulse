---
phase: 17-v4.0-quality-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build.py
  - content/tools_crm.py
  - content/tools_outbound.py
  - content/tools_enrichment.py
  - content/tools_automation.py
  - content/roundups_category.py
autonomous: true
requirements: [QUAL4-01, QUAL4-02]

must_haves:
  truths:
    - "All 20 insight articles pass validation (title, word count, links, schema)"
    - "Zero warnings from full build across all 284 pages"
    - "No broken internal links across the entire site"
  artifacts:
    - path: "scripts/build.py"
      provides: "QUAL4-01 Article schema check and broken internal link check in validate_pages()"
      contains: "QUAL4-01"
    - path: "content/tools_crm.py"
      provides: "Word count fixes for close, attio, pipedrive reviews"
    - path: "content/tools_outbound.py"
      provides: "Word count fixes for salesloft, outreach, smartlead reviews"
    - path: "content/tools_enrichment.py"
      provides: "Word count fix for fullenrich review"
    - path: "content/roundups_category.py"
      provides: "Word count fix for best-workflow-automation-tools roundup"
  key_links:
    - from: "scripts/build.py validate_pages()"
      to: "output/insights/*/index.html"
      via: "QUAL4-01 Article schema check"
      pattern: "QUAL4-01.*insight article missing Article schema"
    - from: "scripts/build.py validate_pages()"
      to: "all internal hrefs"
      via: "broken link file-existence check"
      pattern: "QUAL4-02.*broken internal link"
---

<objective>
Fix all 8 word count warnings, add Article JSON-LD schema validation for insight pages, add broken internal link detection, and confirm a zero-warning build across all 284 pages.

Purpose: Close out v4.0 quality sweep so the site is ready for go-live with zero validation warnings.
Output: Zero-warning build, two new validation checks in validate_pages().
</objective>

<execution_context>
@/Users/rome/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rome/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-v4.0-quality-sweep/17-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add QUAL4 validation checks and fix word count warnings</name>
  <files>scripts/build.py, content/tools_crm.py, content/tools_outbound.py, content/tools_enrichment.py, content/tools_automation.py, content/roundups_category.py</files>
  <action>
Two parts:

**Part A — Add two new validation checks to validate_pages() in build.py:**

1. QUAL4-01: Article JSON-LD schema check for insight pages. Insert after the QUAL3-02 block (after line 14544), before the QUAL2-07 duplicate detection block. Pattern:
```python
# --- QUAL4-01: Article schema on insight pages ---
if re.match(r"insights/[^/]+/index\.html$", rel) and rel != "insights/index.html":
    if '"article"' not in html_lower and '"@type": "article"' not in html_lower:
        warnings.append(f"QUAL4-01: {rel}: insight article missing Article schema")
```

2. QUAL4-02: Broken internal link detection. Add after the QUAL2-07 duplicate detection block (after line 14552), before the final warning output. This is a post-loop check:
- First, build a set of all valid paths from the output directory (walk output/, compute relative paths, normalize to URL paths like `/salary/`, `/tools/clay-review/`).
- Then, for each page's HTML that was already read in the per-file loop, collect all internal `href` values (links starting with `/` but not `//`). Store these as a dict of `{page_path: [list of hrefs]}` during the per-file loop.
- After the loop, check each href against the valid paths set. Warn on any link whose target doesn't exist as a file in output/.
- Exclude anchors (strip `#` and everything after), query strings (strip `?` and everything after), and external links.
- Normalize: `/foo/` should match `foo/index.html` in output.

**Part B — Fix 8 word count warnings by adding substantive content:**

Each page needs 5-15 extra words of genuinely useful content. Add one sentence to each tool's content module expanding a specific section (overview, gtm_use_cases, or criticism). Do NOT use banned words from CLAUDE.md. Do NOT add filler.

Files and tools to fix:
- `content/tools_outbound.py`: salesloft (990 words, need 10+), outreach (990, need 10+), smartlead (991, need 9+)
- `content/tools_enrichment.py`: fullenrich (997, need 3+)
- `content/tools_crm.py`: close (996, need 4+), attio (992, need 8+), pipedrive (992, need 8+)
- `content/roundups_category.py`: best-workflow-automation-tools (999, need 1+)

For each, add a sentence about pricing detail, a specific integration, a concrete use case, or an honest limitation. Keep the GTME Pulse voice: direct, specific, practitioner-first.
  </action>
  <verify>
    <automated>cd /Users/rome/Documents/projects/gtmepulse && python3 scripts/build.py --skip-og 2>&1 | tail -10</automated>
  </verify>
  <done>Build output shows "Content validation: all clear" with zero warnings. QUAL4-01 check exists for Article schema. Broken internal link check exists. All 8 previously-warning pages now pass word count threshold.</done>
</task>

<task type="auto">
  <name>Task 2: Full build verification with OG images</name>
  <files>scripts/build.py</files>
  <action>
Run the full build (without --skip-og) to confirm:
1. All 284 pages build successfully
2. Zero warnings from validate_pages()
3. OG images generate correctly after content changes
4. "Content validation: all clear" message in output

This is a verification-only task. No code changes unless the full build reveals issues (in which case fix them).

Run: `cd /Users/rome/Documents/projects/gtmepulse && python3 scripts/build.py 2>&1 | grep -E "WARNING|all clear|Build complete|Error"`

If any warnings appear, fix them before marking done.
  </action>
  <verify>
    <automated>cd /Users/rome/Documents/projects/gtmepulse && python3 scripts/build.py 2>&1 | grep -E "WARNING|all clear|Build complete"</automated>
  </verify>
  <done>Full build (with OG images) completes with "Content validation: all clear" and "Build complete: 284 pages". Zero warnings.</done>
</task>

</tasks>

<verification>
1. `python3 scripts/build.py --skip-og 2>&1 | grep WARNING` returns empty (zero warnings)
2. `python3 scripts/build.py --skip-og 2>&1 | grep "all clear"` returns "Content validation: all clear"
3. `python3 scripts/build.py 2>&1 | grep "Build complete"` shows 284 pages
4. grep for "QUAL4-01" in build.py confirms Article schema check exists
5. grep for "broken internal link" or "QUAL4-02" in build.py confirms link check exists
</verification>

<success_criteria>
- Zero warnings from `python3 scripts/build.py` (full build with OG)
- All 20 insight articles pass validation including Article JSON-LD schema check
- Broken internal link detection is active and finds zero broken links
- 284 pages build successfully
</success_criteria>

<output>
After completion, create `.planning/phases/17-v4.0-quality-sweep/17-01-SUMMARY.md`
</output>
