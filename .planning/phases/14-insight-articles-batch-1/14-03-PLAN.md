---
phase: 14-insight-articles-batch-1
plan: 03
type: execute
wave: 3
depends_on: [14-02]
files_modified:
  - scripts/build.py
autonomous: true
requirements: [ART-08, ART-09, ART-10]

must_haves:
  truths:
    - "LinkedIn outreach playbook is accessible at /insights/linkedin-outreach/"
    - "Email deliverability guide is accessible at /insights/email-deliverability/"
    - "API integration guide is accessible at /insights/api-integration/"
    - "Each article has Article JSON-LD with Person author markup"
    - "Each article has 1,500-2,500 words with 3+ internal links and 2+ outbound citations"
    - "BUILT_INSIGHT_SLUGS contains all 10 slugs"
    - "Insights index page shows all 10 articles"
    - "Full build completes with zero warnings across all ~273+ pages"
  artifacts:
    - path: "scripts/build.py"
      provides: "3 final insight article functions, BUILT_INSIGHT_SLUGS complete (10 entries)"
      contains: "build_insight_linkedin_outreach"
  key_links:
    - from: "scripts/build.py"
      to: "output/insights/linkedin-outreach/"
      via: "write_page()"
      pattern: "write_page.*insights/linkedin-outreach"
    - from: "scripts/build.py"
      to: "output/insights/email-deliverability/"
      via: "write_page()"
      pattern: "write_page.*insights/email-deliverability"
    - from: "scripts/build.py"
      to: "output/insights/api-integration/"
      via: "write_page()"
      pattern: "write_page.*insights/api-integration"
---

<objective>
Build the final 3 insight articles: LinkedIn outreach automation playbook (ART-08), email deliverability guide (ART-09), and API integration patterns (ART-10). Complete the BUILT_INSIGHT_SLUGS set so all 10 articles appear on the insights index.

Purpose: Completes Phase 14 with the remaining playbook articles covering LinkedIn, email, and API integration patterns.
Output: 3 new article pages, all 10 insight articles live, insights index complete.
</objective>

<execution_context>
@/Users/rome/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rome/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-insight-articles-batch-1/14-RESEARCH.md
@.planning/phases/14-insight-articles-batch-1/14-02-SUMMARY.md

<interfaces>
<!-- Infrastructure from Plan 01, expanded in Plan 02 -->

From scripts/build.py:
```python
INSIGHT_PAGES = [...]  # 10 entries
BUILT_INSIGHT_SLUGS = {"job-market-2026", "salary-trends", "tool-adoption", "state-of-gtme-2026", "clay-ecosystem", "outbound-stack", "clay-playbook"}
def insight_related_links(current_slug):
def pad_description(desc, target_min=150, target_max=158):
def source_citation_html():
def breadcrumb_html(crumbs):
def newsletter_cta_html(context=""):
```

From scripts/templates.py:
```python
def get_article_schema(title, description, slug, date_published, word_count):
def get_breadcrumb_schema(items):
def get_page_wrapper(title, description, canonical_path, body_content, ...):
def write_page(rel_path, content):
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Articles ART-08 (LinkedIn outreach) and ART-09 (email deliverability)</name>
  <files>scripts/build.py</files>
  <action>
Add 2 `build_insight_*()` functions. Add their slugs to BUILT_INSIGHT_SLUGS. Add build calls to main().

**ART-08: build_insight_linkedin_outreach()** — "LinkedIn Outreach Automation for GTM Engineers"
- Slug: "linkedin-outreach"
- Cover: Practical playbook for LinkedIn-based prospecting as a GTM Engineer:
  1. Sales Navigator setup and search strategies (boolean filters, saved searches, lead lists)
  2. Automation tools overview: PhantomBuster (scraping + messaging), HeyReach (multi-account), Expandi (cloud-based), Dripify (sequences)
  3. Connection request strategy (personalization at scale, acceptance rates by message type)
  4. Multi-channel sequencing: LinkedIn + email coordination (timing, message variation)
  5. Compliance and risk: LinkedIn's automation policy, account warming, daily limits, proxy usage
  6. Measuring LinkedIn outreach (connection rate, reply rate, meeting conversion)
  7. When LinkedIn outreach works vs when email wins
- Stat cards: 30% avg connection rate, 300 connections/week safe limit, 3-5x reply rate vs cold email
- Internal links to: /tools/category/linkedin-social/, /comparisons/heyreach-vs-expandi/, /insights/outbound-stack/, /insights/email-deliverability/, /careers/how-to-become-gtm-engineer/, /glossary/outbound-sequencing/
- Outbound: LinkedIn's official automation policy, HeyReach's multi-account safety guide

**ART-09: build_insight_email_deliverability()** — "Email Deliverability for GTM Engineers"
- Slug: "email-deliverability"
- Cover: Technical guide to email deliverability for outbound:
  1. Domain setup (secondary domains, SPF, DKIM, DMARC records, DNS configuration)
  2. Warmup strategy (mailbox warmup tools, volume ramp schedule, 14-day minimum)
  3. Reputation management (sender score, blacklist monitoring, bounce rate thresholds)
  4. Content that lands in inbox (plain text vs HTML, link tracking impact, spam trigger words)
  5. Infrastructure: Instantly vs Smartlead deliverability features, email validation (ZeroBounce, NeverBounce)
  6. Domain rotation strategy (3-5 domains, 50-100 emails/domain/day)
  7. Monitoring and troubleshooting (GlockApps, Mail Tester, inbox placement testing)
  8. The cold email math: domains x mailboxes x daily volume = total capacity
- Stat cards: 3-5 domains minimum, 14-day warmup, 50-100/domain/day, <2% bounce target
- Internal links to: /tools/category/outbound-sequencing/, /comparisons/instantly-vs-smartlead/, /insights/outbound-stack/, /insights/linkedin-outreach/, /glossary/email-deliverability/, /tools/instantly-review/
- Outbound: Google's bulk sender guidelines, Instantly's deliverability documentation

Each: 1,500-2,500 words, salary-header + salary-stats + salary-content pattern, source_citation_html(), newsletter_cta_html(), insight_related_links(), Article JSON-LD, breadcrumb schema.

CRITICAL: No em-dashes. No banned words. No false reframes. Practitioner voice. Contractions always.
  </action>
  <verify>
    <automated>cd /Users/rome/Documents/projects/gtmepulse && python3 scripts/build.py 2>&1 | tail -30</automated>
  </verify>
  <done>
    - /insights/linkedin-outreach/ and /insights/email-deliverability/ build without warnings
    - Both have Article JSON-LD, 1,500+ words, 3+ internal links, 2+ outbound citations
  </done>
</task>

<task type="auto">
  <name>Task 2: Article ART-10 (API integration) and final completion</name>
  <files>scripts/build.py</files>
  <action>
Add `build_insight_api_integration()` function. Add slug to BUILT_INSIGHT_SLUGS (now all 10 slugs present). Add build call to main(). Verify BUILT_INSIGHT_SLUGS matches all 10 INSIGHT_PAGES slugs exactly.

**ART-10: build_insight_api_integration()** — "API Integration Patterns for GTM Engineers"
- Slug: "api-integration"
- Cover: Technical guide to API-driven GTM automation:
  1. APIs GTM Engineers use most (CRM APIs, enrichment APIs, sequencer APIs, LLM APIs)
  2. Authentication patterns (API keys, OAuth 2.0, bearer tokens, webhook signatures)
  3. Webhook-driven automation (inbound webhooks from CRM, processing pipelines, response patterns)
  4. Zapier/Make vs custom Python scripts (when each wins, cost comparison, maintenance burden)
  5. Building a Python enrichment script (requests, error handling, rate limiting, data persistence)
  6. n8n and Make workflow patterns (HTTP nodes, data transformation, conditional branching)
  7. Error handling and retry strategy (exponential backoff, circuit breakers, dead letter queues)
  8. Cost optimization (caching API responses, batch vs single calls, credit management)
- Stat cards: 5-8 APIs in avg stack, Python in 42% of job postings, 3 integration approaches
- Internal links to: /tools/category/workflow-automation/, /comparisons/make-vs-zapier/, /comparisons/make-vs-n8n/, /insights/outbound-stack/, /insights/clay-ecosystem/, /careers/skills/python/
- Outbound: Make's API documentation, Zapier's automation platform comparison

1,500-2,500 words. This is the most technical article in the batch. Include code-formatted examples (using <code> or <pre> tags for API call patterns, not actual runnable code). Write for GTM Engineers who are learning to code, not senior developers.

After building ART-10, verify:
- BUILT_INSIGHT_SLUGS has exactly 10 entries matching all INSIGHT_PAGES slugs
- All 10 build_insight_*() calls exist in main()
- Full build runs with zero warnings

CRITICAL: No em-dashes. No banned words. No false reframes. Practitioner voice.
  </action>
  <verify>
    <automated>cd /Users/rome/Documents/projects/gtmepulse && python3 scripts/build.py 2>&1 | tail -30</automated>
  </verify>
  <done>
    - /insights/api-integration/ builds without warnings
    - BUILT_INSIGHT_SLUGS has all 10 slugs
    - Insights index shows all 10 articles
    - Full build produces zero warnings across all ~273+ pages
    - All 10 articles have Article JSON-LD, meet word count targets, have required links
  </done>
</task>

</tasks>

<verification>
- `python3 scripts/build.py` completes with zero warnings
- `ls output/insights/*/index.html | wc -l` returns 10 (excluding insights/index.html)
- `grep -c "application/ld+json" output/insights/linkedin-outreach/index.html` returns 2+ (breadcrumb + article)
- `grep -c "application/ld+json" output/insights/email-deliverability/index.html` returns 2+
- `grep -c "application/ld+json" output/insights/api-integration/index.html` returns 2+
- Insights index page lists all 10 articles
- Total page count increased by 11 (1 index + 10 articles)
</verification>

<success_criteria>
- All 10 insight articles live at /insights/[slug]/ with Article JSON-LD
- Each meets 1,500-2,500 word target, has 3+ internal links and 2+ outbound citations
- Insights index page shows all 10 articles with category badges
- Full site builds with zero warnings
- Phase 14 requirements ART-01 through ART-10 all satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/14-insight-articles-batch-1/14-03-SUMMARY.md`
</output>
